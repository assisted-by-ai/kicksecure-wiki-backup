{{Header}}
{{title|title=
Progress Reports 2
}}
{{#seo:
|description=Overview on the continuous progress for Kicksecure (and Whonix) with individual specific contributions for content, research, implementation etc
|image=Page-progress-reports-thumb.jpg
}}
{{devwiki}}
{{about_mininav}}
<div class="mininav">
* [[#arraybolt3|arraybolt3]]
* [[#Hans|Hans]]
* [[#nurmagoz|nurmagoz]]
</div>
[[File:Page-progress-reports-thumb.jpg|thumb|200px]]
{{intro|
On this page we give an overview on the continuous progress for Kicksecure (and Whonix) on the most active contributors.
}}

= arraybolt3 =
== 2026-05-08 ==
=== Write sandbox-manager-dist #3 ===
Date: 2026-05-08

Continued work on the protocol handler.

=== Review libvirt feature request for UNIX socket networking, test a possible existing solution ===
Date: 2026-05-08

Discovered vhost-user networking but was unable to get it working. Left a comment on an existing feature request documenting why UNIX-socket based internal networking would be useful.

== 2026-05-07 ==
=== Write sandbox-manager-dist #2 ===
Date: 2026-05-07

Continued work on the protocol handler, polishing the design as I went.

=== Fix qubesdb write restriction PR ===
Date: 2026-05-07

Fixed the SELinux-induced failure on Fedora.

=== Routine tasks, determine that Dirty Frag affects Kicksecure ===
Date: 2026-05-07

A new Copy Fail-like kernel vulnerability was mentioned on the oss-security mailing list. Successfully used the exploit code on Kicksecure 18. Drafted a news announcement and shared it with Patrick for him to post if he'd like to.

== 2026-05-06 ==
=== Write sandbox-manager-dist #1 ===
Date: 2026-05-06

Currently working on the protocol handler.

=== File PR for restricting qubesdb write permissions ===
Date: 2026-05-05

Tested on Kicksecure, works. Marek asked me to rename a socket, and then later noticed SELinux failures on Fedora.

== 2026-05-05 ==
=== Start adjusting qubesdb to restrict write permissions ===
Date: 2026-05-05

Identified most of the parts that need to change to restrict write permissions to the qubes group. Made a lot of the necessary changes.

=== Further design of sandbox-app-launcher #5 ===
Date: 2026-05-05

Finished the initial design iteration. Discussed some aspects of the design with Marek (no input other than noting that some implementations like Flatpak and bwrap already exist, and warning that sandboxing is a rather weak measure, which we agree with but still believe this work is worthwhile).

== 2026-05-04 ==
=== Further design of sandbox-app-launcher #4 ===
Date: 2026-05-04

Much more of the protocol is now designed, more missing UI screens added. Shared on the forums and with Marek.

=== Rework debug-kicksecure service addition for Qubes CI ===
Date: 2026-05-04

Scrapped the original Perl implementation in favor of a much simpler Python one at Marek's suggestion.

=== Investigate a failed Qubes environment test ===
Date: 2026-05-04

Turned out the test failure was a red herring, but I did find some strange-looking behavior that might be relevant if another test failure pops up in the future.

== 2026-05-03 ==
=== Further design of sandbox-app-launcher #3 ===
Date: 2026-05-03

Mostly focused on getting the UI design close to finished, the backend will need some design changes to accommodate it.

=== Discover that the systemd-journald sanitization issue has a CVE assigned ===
Date: 2026-05-03

Was going to report the vuln to get a CVE assigned, but discovered one has already been reported.

=== First attempt at adding a debug-kicksecure service to Qubes CI ===
Date: 2026-05-03

Submitted a PR, got feedback from upstream, will probably need reworked.

== 2026-05-02 ==
=== Further design of sandbox-app-launcher #2 ===
Date: 2026-05-02

Continuing to map out how the sandbox system should work under the hood, especially to make file and folder copying between host and sandbox secure.

== 2026-05-01 ==
=== Further design of sandbox-app-launcher ===
Date: 2026-05-01

Mapped out more of the user interface, thought through several design decisions and started laying out the details of the API between the frontend and backend.

=== Review sq-git tag verification feature, submit PR with additional regression tests ===
Date: 2026-05-01

Feature appears to work properly. New tests pass locally. Submitted PR upstream, it was accepted but is currently pending merge due to external factors.

== 2026-04-30 ==
=== Continue redesign of sandbox-app-launcher ===
Date: 2026-04-30

Mapped out more behavior, features, window design, started to think about how the backend needed to work. Currently its designed as a run-as-root daemon, which is a potential security liability, so I'm intending on trying to run a full Linux container inside bwrap instead to see if this can be avoided.

=== Fix wlr-resize-watcher not handling disabled displays properly ===
Date: 2026-04-30

Claude attempted a bugfix for this, but it looked like it was looking for a hallucinated value. Implemented and tested a working bugfix.

=== Review Claude bugfixes and agent documentation ===
Date: 2026-04-30

Carefully read through several new fixes and agent documentation commits submitted by Claude, commented on them, integrated some changes, documented why others were rejected.

=== File Glycin abstraction PR against AppArmor upstream ===
Date: 2026-04-30

roddhjav approved the adapted profiles I took from apparmor.d. This is now submitted upstream to AppArmor for review. Pinged both roddhjav and intrigeri (Debian's AppArmor maintainer) to take a look.

=== Fix legacy-dist.service restarting during upgrades ===
Date: 2026-04-30

legacy-dist.service needs to only run during bootup, restarting during an upgrade can potentially cause problems. Made dh_installsystemd stop restarting legacy-dist.service on upgrades, and version-bumped functions that may need to re-run.

=== File Qubes issue for adding more detailed debugging flags ===
Date: 2026-04-30

Patrick would like for the Qubes CI to have a flag somewhere that Kicksecure can read to determine if it is running in CI, and enable extra diagnostics if so. Discussed with Marek how this could work, filed a Qubes issue for it and suggested a stop-gap solution so we don't have to wait for a large refactor before getting this done.

=== Review code changes from Claude hardening refactor #5 ===
Date: 2026-04-30

More review of new code. Discussed with Patrick, further review will be halted until the refactor is complete on his end, then review will pick up where it left off.

== 2026-04-29 ==
=== Review code changes from Claude hardening refactor #4 ===
Date: 2026-04-29

Read through quite a bit more of the new code, made some small changes locally.

=== Research copy.fail mitigation safety, share findings on oss-security ===
Date: 2026-04-29

Concluded that the vulnerable part of the kernel crypto API was not universally safe to disable, and mentioned this on the oss-security ML. Patrick referenced my email in other documentation.

=== Add Reviewed-By lines to GRUB fix for backing out of menus with auth enabled ===
Date: 2026-04-29

This was the last requirement before the PR could be merged, and now it is merged.

=== Fix formatting on Qubes environment handling integration tests ===
Date: 2026-04-29

Fixed the formatting issues Black complained about.

== 2026-04-28 ==
=== Review code changes from Claude hardening refactor #3 ===
Date: 2026-04-28

Finished reading through everything, pushed fixes.

=== Attempt to fix integration tests for environment handling in Qubes ===
Date: 2026-04-28

Fixed the remaining known issues, however the new code apparently needs reformatted by Black.

=== Fix Qubes template build failures caused by DKMS suppression code ===
Date: 2026-04-28

A <code>mkdir</code> call was missing from the DKMS suppression code in our template build scripts. Added it.

== 2026-04-27 ==
=== Review code changes from Claude hardening refactor #2 ===
Date: 2026-04-27

Continuing to read through code and make fixes where needed.

== 2026-04-26 ==
=== Review code changes from Claude hardening refactor #1 ===
Date: 2026-04-26

Patrick is doing a large hardening refactor across large portions of Kicksecure's codebase. I reviewed a lot of the changes, and have some minor fixes locally that I'm working on.

=== More Bash code injecton prevention #4 ===
Date: 2026-04-26

As discussed.

== 2026-04-25 ==
=== More Bash code injection prevention #3 ===
Date: 2026-04-25

As discussed.

=== Fix metapackage check in systemcheck ===
Date: 2026-04-25

Added the CLI family metapackages to the lists of metapackages to check for.

=== Review, merge zsh fixes from Claude ===
Date: 2026-04-25

This fixes an issue with PATH being overridden, and prevents prompt character injection in a few contexts.

== 2026-04-24 ==
=== More Bash code injection prevention #2 ===
Date: 2026-04-24

As discussed.

== 2026-04-23 ==
=== More Bash code injection prevention #1 ===
Date: 2026-04-23

As discussed.

=== Adjust AppArmor policies for Glycin in cooperation with roddhjav ===
Date: 2026-04-23

roddhjav (apparmor.d's maintainer) requested that I make several changes to the adapted Glycin profiles I sent him for review. Made those changes and pinged him to ask for re-review.

=== Test vboxdrv kernel oops patch from Oracle ===
Date: 2026-04-23

A VBox developer posted a workaround patch for me to test. I applied it, tested it, and it appeared to work. Further discussion is happening upstream on which approach should be taken to resolve the issue.

=== grub-pwchange authentication PR comment ===
Date: 2026-04-23

Upstream was unsure if the patch I submitted for fixing backing out of submenus with authentication enabled was safe. Looked at the code a bit, left a comment explaining how it appeared variable initialization was working.

== 2026-04-22 ==
=== More sandbox-app-launcher design ===
Date: 2026-04-22

Started experimenting with systemd-nspawn, determined that we would need a privileged backend to make this work, and started fleshing out more of the UI and how the CLI would have to work.

=== Report VirtualBox kernel oops bug related to kernel.kptr_restrict=2, document workaround ===
Date: 2026-04-22

VirtualBox is causing a kernel oops when the vboxdrv module is loaded on Kicksecure. Figured out part of why, documented a workaround, and reported the issue upstream.

=== Attempt to reproduce sysmaint-panel crash with display scaling ===
Date: 2026-04-22

Could not reproduce in a VM or on baremetal.

=== Fix PCManFM-Qt integration with lxqt-archiver ===
Date: 2026-04-22

PCManFM-Qt was trying to use xarchiver by default due to a configuration mistake. Fixed, mentioned a potential follow-up task to Patrick in chat.

== 2026-04-21 ==
=== Start designing sandbox-manager-dist ===
Date; 2026-04-21

(Name may change, maybe we just call this sandbox-app-launcher, but it does more than just launching and sandboxing apps.) Designed a basic UI and created a necessary feature list.

=== Stop DKMS key generation during Qubes template builds, enable legacy-dist debugging in Qubes CI ===
Date: 2026-04-21

Added DKMS key generation suppressing code to the template build script, added a snippet to Qubes' OpenQA test code that enables <code>set -x</code> in legacy-dist's fixes script.

=== Test live-build deb822 MR changes ===
Date: 2026-04-21

Successfully built an ISO, booted it, and verified that it used deb822 sources properly.

== 2026-04-20 ==
=== Apply more requested changes to the live-build deb822 MR ===
Date: 2026-04-20

Roland requested that I remove one-line sources support entirely and fix a few things in the PR. Did so, pushed the code. Still needs tested.

=== Investigate DKMS key generation issues in Qubes OS ===
Date: 2026-04-20

Marek pinged me about a popup from systemcheck warning that our DKMS key rotation code was being called and was malfunctioning. Investigated possible causes, created a follow-up ticket.

=== Fix potential Bash code injection risks in the codebase ===
Date: 2026-04-20

Found several places where variable dereferencing could be a potential code injection risk, fixed all areas for safety's sake.

=== More work on Glycin + AppArmor ===
Date: 2026-04-20

Got a working profile, but it probably needs changes before attempting to upstream it. Collaborating with roddhjav to figure out how to do this correctly.

== 2026-04-19 ==
=== Mostly figure out how to fix Glycin + AppArmor in Debian Testing ===
Date: 2026-04-19

Researched, experimented, got a too-permissive-but-mostly-working profile set created. Will submit to upstream and request review from the apparmor.d maintainer once complete.

=== Improve fm-shim's Unicode support ===
Date: 2026-04-19

fm-shim now allows file paths with Unicode in them, but hides and warns about the Unicode characters. It also allows users to display them anyway, and allows users to analyze them if desired.

=== Create run-via-xwayland wrapper ===
Date: 2026-04-19

This simply exports a couple of environment variables so that applications will try to use XWayland rather than Wayland, then runs the specified app.

== 2026-04-18 ==
=== Add better logging to fm-shim frontend ===
Date: 2026-04-18

The shim now says what paths failed validation, and why.

=== Fix integration tests for Qubes environment fixes ===
Date: 2026-04-18

Implemented the change requests from Marek.

=== Merge kloak fix from sotormd for non-Debian platforms ===
Date: 2026-04-18

Verified that their suggested anchor fix worked right on Whonix-Workstation 18, merged and pushed it.

=== Claude PR review #13 ===
Date: 2026-04-18

Reviewed, tested, and merged a derivative-maker PR that reverted an unnecessary change I had made to git_sanity_test, simplified some code, and moved most of the verbose comments into documentation files.

== 2026-04-17 ==
=== Review PR to fix kloak failing to start on non-Debian platforms ===
Date: 2026-04-17

Could not initially determine how the issue made sense, found that the initially proposed fix didn't work. The contributor has updated their report with some more info, so I'll be coming back to this soon.

=== Initial attempt at writing integration tests for Qubes environment fixes ===
Date: 2026-04-17

Marek left some review notes, the tests aren't quite correct but they're close.

=== Review registered tester and AI use policy wiki pages ===
Date: 2026-04-17

AI policy looked fine, made some small changes to the registered tester docs.

=== Fix kloak issues reported by Coverity Scan ===
Date: 2026-04-17

No vulnerabilities or serious bugs were found.

=== Claude PR review #12 ===
Date: 2026-04-17

Reviewed, tested, and merged a fix for helper-scripts fixing a regex catastrophic backtracking issue.

== 2026-04-16 ==
=== Claude PR review #11 ===
Date: 2026-04-16

Finished reviewing the derivative-maker PR that rewrote git_sanity_test, added cryptographic git tag verification on top of the code Claude wrote and fixed some other bugs.

== 2026-04-15 ==
=== Claude PR review #10 ===
Date: 2026-04-15

Reviewed and merged a helper-scripts PR, got partway through a PR on derivative-maker that will need some rework.

== 2026-04-14 ==
=== Disable systemd-journald ForwardToWall ===
Date: 2026-04-14

This mitigates the terminal injection vulnerability we found some months ago.

=== Claude PR review #9 ===
Date: 2026-04-14

Finished work on the second stack of PRs.

== 2026-04-13 ==
=== Claude PR review #8 ===
Date: 2026-04-13

Made progress integrating the second stack of PRs.

=== Reproduce wlr-resize-watcher resize bug, submit suggested fix ===
Date: 2026-04-13

Was finally able to reproduce the issue that was reported on the forums (the watcher was not shrinking the display to 1920x1080 in one edge case). Also determined that their suggested fix would work, and added it to my arraybolt3/trixie branch.

== 2026-04-12 ==
=== Claude PR review #7 ===
Date: 2026-04-12

Started working on the second stack of PRs.

=== Investigate possible wlr-resize-watcher display resize bug ===
Date: 2026-04-12

Have not yet been able to reproduce the issue or properly ascertain what the issue is.

=== File PR to fix qube environment to unbreak fm-shim ===
Date: 2026-04-12

This just syncs the environment from the user session back into the systemd user manager and dbus-daemon, so that applications that rely on the environment stored by those services work properly. The fm-shim system in security-misc is one such application.

=== Integrate check-ref-comments-for-unicode into dm-packaging-helper-script, fix changelog.upstream generation ===
Date: 2026-04-12

Now branches merged into our core repositories will be automatically checked for malicious Unicode at merge time. Because the format of changelog.upstream was liable to trigger the Unicode checker with trailing spaces, I added a fix to genmkfile so that it generates changelog.upstream files without trailing spaces.

=== Create modeline-show and text-safety-scan scripts ===
Date: 2026-04-12

This is an implementation of the text-safety-scan idea mentioned below.

== 2026-04-11 ==
=== Create Git PR unicode check script ===
Date: 2026-04-11

The script, called <code>check-ref-commits-for-unicode</code>, will allow us to see if there are unsafe characters in pull requests submitted by human and AI contributors. It finds all commits reachable from one ref that aren't reachable from another, and scans them for Unicode characters.

=== Review kloak-related concerns found by Codex ===
Date: 2026-04-11

Fixed a couple of possible integer overflow bugs.

=== Discuss creating text-safety-scan ===
Date: 2026-04-11

This is planned to be a wrapper script that will call unicode-show and a text editor modeline detector on passed-in files. We can add additional safety check plugins to this going forward.

=== Routine code review, discuss safeguards around AI and GitHub Actions use ===
Date: 2026-04-11

Shared notes in chat, ensured that we don't have anything that can automatically push commits to our repos and noted some concerns around using API keys in some contexts.

== 2026-04-10 ==
=== Remove Vim and Emacs modelines from codebase and prevent future reintroduction ===
Date: 2026-04-10

Removed all modelines from our code except from grml-debootstrap. Added code to dm-check-unicode to detect modelines and reject them.

=== Claude PR review #6 ===
Date: 2026-04-10

Finished working through the first stack of PRs.

== 2026-04-09 ==
=== Claude PR review #5 ===
Date: 2026-04-09

Continuing to work through the list of PRs.

=== Regular tasks, review AI bug reports on kloak ===
Date: 2026-04-09

Codex pointed out a couple potential issues in kloak, neither of which appeared to be problematic. Also did security-related email followups, forum replies, and review of new code committed by Patrick.

== 2026-04-08 ==
=== Claude PR review #4 ===
Date: 2026-04-08

Spent most of the time reviewing and integrating changes to dist-installer-cli.

=== Polish Qubes AppVM retagging PRs ===
Date: 2026-04-08

A couple days ago, Marek asked me to make changes to the PRs to qubes-core-admin-addon-kicksecure and qubes-core-admin-addon-whonix to remove tags from VMs that don't need them anymore. Implemented these changes now.

=== Prepare request to systemd upstream to allow republishing systemd-journald input sanitization bug report ===
Date: 2026-04-08

Sent to Patrick for review.

== 2026-04-07 ==
=== Claude PR review #3 ===
Date: 2026-04-07

Reviewed and merged more fixes from Claude.

=== Follow up on security report, publicly disclose systemd-journald input sanitization vulnerability ===
Date: 2026-04-07

Reported the systemd-journald issue on the systemd bug tracker and on the oss-security openwall mailing list. Unfortunately the systemd bug report was deleted by upstream. Also followed up on a different security-related email.

== 2026-04-06 ==
=== Claude PR review #2 ===
Date: 2026-04-06

Fixed more issues, added some more compiler flags to our documentation. (These will need to be propagated to all of our C code.)

=== Investigate sdwdate timeout issue ===
Date: 2026-04-06

Looks like migrating from using a shell-based sleep mechanism to using Python's <code>time.sleep()</code> didn't ultimately work after all. We should probably go back to using shell-based sleep.

=== Review documentation for safely using <code>find</code> ===
Date: 2026-04-06

Approach looks sound, didn't see any issues. Removed the TODO note from the wiki chapter since it has now been reviewed.

== 2026-04-05 ==
=== Claude PR review #1 ===
Date: 2026-04-05

Reviewed a bunch of PRs from Claude Code, implementing various suggested fixes. Kept track of what changes were accepted and what changes weren't.

=== More sdwdate-gui missing menu entries work ===
Date: 2026-04-05

Fixed some issues found by an AI code review, found a memory leak bug and fixed that also.

=== Document why we set <code>QT_QPA_PLATFORM=waylamd</code> and <code>GDK_BACKEND=wayland</code> in desktop-config-dist ===
Date: 2026-04-05

Discussed with Patrick, replied to a bug report, created a commit documenting the reason for those environment variables.

== 2026-04-04 ==
=== Fix kloak lockup when using manual login ===
Date: 2026-04-04

The new mechanism for detecting when the Wayland compositor has started means that kloak may connect to the compositor that displays the login screen. It was failing to detect when that compositor terminated, resulting in all keyboard and mouse input being swallowed once login was complete. Fixed that issue and a few other issues.

=== Further testing of AI PRs for sdwdate-gui missing menu entries issue ===
Date: 2026-04-04

Things seem to work after testing them some more. During deeper review I found and fixed a possible memory leak issue.

== 2026-04-03 ==
=== Review AI PRs to fix sdwdate-gui missing menu entries ===
Date: 2026-04-03

Used the provided code as inspiration to develop a bugfix. Some of the changes were merged in as-is, but many of them were changed substantially in the process.

=== Review user-sysmaint-split PRs from Claude ===
Date: 2026-04-03

Read through the PRs, integrated the ones that looked useful, adjusting them as necessary.

=== Investigate strange privleap behavior on upgrade, AI PR ===
Date: 2026-04-03

privleap crashed in a very strange way on one machine, after investigation it didn't look like something that could happen "normally". The AI PR suggested for it also didn't look like it would fix any related issues and would introduce a greater DoS risk, therefore it was rejected.

=== Fix sdwdate-gui startup partially failing ===
Date: 2026-04-03

A bad config migration combined with an incorrectly written systemd unit resulted in sdwdate-gui only halfway starting up. Found and fixed the root cause of the issue.

== 2026-04-02 ==
=== Whonix-Installer overhaul #3 ===
Date: 2026-04-02

Started implementing the core installation code, made good progress. Switched to a slightly different cross-thread call mechanism that may be more reliable than message passing.

== 2026-04-01 ==
=== Whonix-Installer overhaul #2 ===
Date: 2026-04-01

Further progress on fixing up Whonix-Installer. Found a solution to a bug where widgets could get scrambled, and laid out most of the archtecture needed to make sure the installation could proceed without running into reentrancy issues which seem to have caused crashes in the past.

== 2026-03-31 ==
=== Whonix-Installer overhaul #1 ===
Date: 2026-03-31

My plans for Whonix-Installer were originally to just fix the build system, fix a bug, and be done. However, after further review, it looks like the Whonix-Installer project is somewhat broken (resources aren't showing up where they should, there's at least one architectural bug, some wording needs changed, code needs to be more defensive) and would benefit from a full audit, so I've started re-implementing it using the original code as a base. This should hopefully prevent us from running into further weird problems with the build system in the future.

=== Reply to intrigeri about AppArmor maintenance ===
Date: 2026-03-31

It looks likely I will end up helping with the maintenance of AppArmor in Debian upstream. intrigeri sent me several tasks to work on and let me know which ones were highest-priority. I replied with some thoughts and let them know I was happy to help after confirming with Patrick that this was OK to spend work time on.

=== Security report followup ===
Date: 2026-03-31

As discussed.

== 2026-03-30 ==
=== Build system fixup for Whonix-Starter and Whonix-Installer #1 ===
Date: 2026-03-30

Patrick ran into issues building Whonix-Installer due to its build system being outdated (Whonix-Starter no longer has an MSI installer, which ended up being a problem). Started work on fixing this, primarily polishing the Whonix-Starter build system to make sure adjustments made to Whonix-Installer wouldn't end up running into more problems later.

=== Security bug reports ===
Date: 2026-03-30

As discussed.

== 2026-03-29 ==
=== Create proof-of-concept for hiding host resolution from a Kicksecure qube and using VNC to access the qube instead of seamless mode ===
Date: 2026-03-29

Successfully got a setup to work that (almost) prevents the host's resolution from leaking to the guest. Ultimately for this to really work, it will need some changes to the Qubes GUI daemon and will require special TigerVNC configuration (or possibly the use of a different VNC viewer), but it's definitely possible to do this. Shared proof-of-concept steps with Patrick and asked Marek if it would be OK to try to contribute this feature upstream.

=== Final fix on the anon-timezone Qubes PR ===
Date: 2026-03-29

Ben had asked that I avoid explicitly comparing against the string <code>"1"</code> when deciding whether or not to respect an anon-timezone feature request from a qube. However, that resulted in weird behavior that Marek asked me to revert. Did the revert, the code is now merged.

=== Create GRUB patch to allow backing out of submenus without needing a password ===
Date: 2026-03-29

GRUB had a bug in it where if authentication was enabled, and the user entered a submenu that was marked <code>--unrestricted</code>, they could not exit that submenu later without having a superuser password. Originally I was going to remove the offending authentication check entirely, but further research revealed that it was potentially important in some situations. Filed an MR that only skips the authentication check when exiting a submenu and not in any other situation where the check would usually be called.

=== Investigate tirdad and syncookies ===
Date: 2026-03-29

I initially (and mistakenly) thought that syncookies were rendered non-functional by tirdad, and reported this upstream, but it turned out I was incorrect. However, we may want to disable syncookies anyway, since they may allow deanonymization via SYN flood attacks.

== 2026-03-28 ==
=== AI feature framework discussion with Namecoin developers ===
Date: 2026-03-28

Discussed various aspects of a sys-ai framework, including language, architecture, and API concerns.

=== Review developer-meta-files and genmkfile Sequoia-GPG porting ===
Date: 2026-03-28

Found and reported one minor concern, fixed a few README files that got lost in other repos.

== 2026-03-27 ==
=== Add password protection features to grub-mkconfig upstream #3 ===
Date: 2026-03-27

Got the patches into a state where I was comfortable publishing them for review, and created an MR.

=== Merge in upstream tirdad code to our repo ===
Date: 2026-03-27

Applied all tirdad changes from upstream to our packaging repo.

=== More fixes for AI-discovered and self-discovered kloak bugs ===
Date: 2026-03-27

Patrick got ChatGPT to notice some more issues in kloak, and while fixing those, I discovered several more issues myself. Created fixes for all of them.

== 2026-03-26 ==
=== Add password protection features to grub-mkconfig upstream #2 ===
Date: 2026-03-26

AI review, testing, MR preparation. Found a shortcoming in GRUB's menu navigation code that resulted in the superuser password being requested when it isn't really necessary and filed a bug report for this. I'll be creating a fix for this.

=== Flesh out run0 documentation ===
Date: 2026-03-26

Documented run0's purpose, the shortcomings that kept us from using it instead of privleap, and in what situations Kicksecure users can access it.

== 2026-03-25 ==
=== Add password protection features to grub-mkconfig upstream #1 ===
Date: 2026-03-25

Polished the initial concept, created an initial implementation, polished the intended commit and email messages. Will hopefully have this more thoroughly tested and submitted soon.

=== Create raspi-firmware MR to assist U-Boot support ===
Date: 2026-03-25

Pending review, this will make it possible to install U-Boot and keep it up to date once it is switched to as the default boot flow (hopefully).

=== Security research bug cleanup ===
Date: 2026-03-25

Followed up on the sha256sum lack of escaping issue, discovered it was already public. Suggested to Patrick that we simplify the list of pending-publication bug reports to avoid leaking upstreams. Researched the CVE reporting process a bit.

== 2026-03-24 ==
=== Tirdad kernel 6.18.17 update review #3 ===
Date: 2026-03-24

Finished a prototype of the testing application, successfully used it to verify that tirdad cloaked ISNs (and on newer kernels TCP timestamp offsets) for both IPv4 and IPv6 connections. Submitted the code for review.

== 2026-03-23 ==
=== Tirdad kernel 6.18.17 update review #2 ===
Date: 2026-03-23

Mostly finished writing the testing application. It isn't quite functional yet, but close.

== 2026-03-22 ==
=== Tirdad kernel 6.18.17 update review #1 ===
Date: 2026-03-22

Read through the code afresh, studied the kernel interfaces it replaces, did not find issues. Had ChatGPT review the code, it found one potential issue which I reported but which turned out not to be an issue after discussion upstream. Started writing a C application for testing the module going forward, since manual testing is hard to get right and is very unintuitive in many ways.

=== Further Sequoia-PGP-related build system fix review ===
Date: 2026-03-22

Reviewed more changes Patrick made, fixed a couple of <code>test</code> statements.

=== Anti-stylometry discussion, investigate more ChatGPT code review reports ===
Date: 2026-03-22

Discussed LLM-based anti-stylometry measures and design with Patrick, getting a rough idea of how it could be implemented securely and efficiently. Looked through several more code review reports from ChatGPT, fixed a few issues.

== 2026-03-21 ==
=== Stop using generic Python window icons for livecheck and sysmaint-panel ===
Date: 2026-03-21

Figured out how to get custom window icons to work right, and implemented it.

=== Review Sequoia-PGP-related build system fixes ===
Date: 2026-03-21

Patrick made several changes to the build system to make its implementation of Sequoia-PGP signature verification a lot easier to use. Reviewed the code, fixed some minor issues, successfully built an ISO with the updated build system.

=== Update mention of user-sysmaint-split in qubes-issues bug report ===
Date: 2026-03-21

There was a Github comment that mentioned how things related to keeping services disabled would work once user-sysmaint-split was functional. This is now deployed, so I updated the comment to reflect that. Also did usual routine work (checking forums, etc.).

== 2026-03-20 ==
=== Verified boot / IMA implementation discussion ===
Date: 2026-03-20

Collaborated with Patrick and another developer on getting IMA-based Verified Boot working. Researched how to implement it, what it was capable of, and what would ultimately be necessary to use it.

=== Security research ===
Date: 2026-03-20

As discussed.

== 2026-03-19 ==
=== Further AI review of file manager shim ===
Date: 2026-03-19

Reviewed some more suggestions from AI code review on the file manager shim, integrated useful changes.

=== Test CSS tracking prevention in Tor Browser ===
Date: 2026-03-19

An anonymous contributor to the wiki informed us that Tor Browser now had protections against CSS-based scriptless user interaction tracking. I tested this, it seemed to kind of work but had some worrying behaviors. Modified the wiki contribution to specify that this mechanism may work, without guaranteeing that it will.

== 2026-03-18 ==
=== Try to figure out how to set a more normal program icon on our Python applets ===
Date: 2026-03-18

A bunch of our applets are using the Python logo as the window logo rather than an application logo. This looks weird. I have partially figured out how to resolve this, but not entirely.

=== AI review of planned raspi-firmware changes ===
Date: 2026-03-18

Ran some changes I made to raspi-firmware through ChatGPT, to find bugs or edge cases that weren't properly handled. These changes will be useful for getting Raspberry Pi images to use UEFI and GRUB by default.

=== Create spec for adding password support to grub-mkconfig ===
Date: 2026-03-18

GRUB does not currently support password-protecting boot entries when using grub-mkconfig. Created a brief specification for how a password protection mechanism could work, so that I can attempt to contribute this to GRUB upstream later.

=== Follow up on security bug reports ===
Date: 2026-03-18

Had a couple of emails to follow up on.

=== Fix kloak compositor detection unreliability that sometimes resulted in failure to cloak inputs ===
Date: 2026-03-18

Removed a clunky, unreliable mechanism for detecting when the compositor is running, and replaced it with a much more reliable polling-based mechanism that should not fail under normal circumstances.

=== More fm-shim polish ===
Date: 2026-03-18

Prevented HTML code from being injected into the frontend UI. Fixed a path resolution bug. Fixed a D-Bus protocol handling bug (failure to send method replies when error conditions are hit).

== 2026-03-17 ==
=== Create fix for kloak numlock issues ===
Date: 2026-03-17

Successfully got Num Lock and the numpad working within VirtualBox on a Whonix-Workstation VM running kloak. The bug ended up being that we weren't handling keyboard LEDs.

== 2026-03-16 ==
=== Start diagnosing kloak numlock issues ===
Date: 2026-03-16

Discovered that kloak is interfering with enabling Num Lock in VirtualBox. Why is unclear, looks like a combination of VirtualBox and kloak bugs. Need to research further.

=== Create MR for policyrcd-script-zg2 ===
Date: 2026-03-16

Marc Haber gave me push access to the policyrcd-script-zg2 repository so I could make a new branch and an MR with the configuration directory support changes. Pushed the new branch, made the MR.

=== Test autologinchange, fix remaining issues ===
Date: 2026-03-16

Created and ran through a test plan, found a couple additional bugs in the process and fixed them.

=== Ageless Linux website PR, notify XDG mailing list of updated age verification plans ===
Date: 2026-03-16

Emailed the XDG mailing list (and others) to mention that we are not planning on making an age verification API anymore and that my initial suggestion is now outdated.

== 2026-03-15 ==
=== Continued autologinchange robustness enhancements ===
Date: 2026-03-15

Patrick put autologinchange through ChatGPT, which found some architectural shortcomings in the original code. Started work on fixing these, made significant progress.

=== Hardening and bugfixing of fm-shim mechanism ===
Date: 2026-03-15

Reviewed the backend and frontend with ChatGPT, finding and fixing several issues and making many improvements in the process, mainly to the frontend Python code.

== 2026-03-13 ==
=== RPi + GRUB + UEFI upstreaming work #3 ===
Date: 2026-03-13

Successfully built an RPi image using Debian's cloud image builder that uses U-Boot and GRUB. Also determined what changes need to be made to raspi-firmware and made most of them (was not able to test them yet). Filed a feature request against the cloud image builder for switching to GRUB by default for Raspberry Pi devices.

== 2026-03-12 ==
=== RPi + GRUB + UEFI upstreaming work #2 ===
Date: 2026-03-12

Filed a bug report about the Trixie boot issues after gathering enough information to file a sufficiently detailed report. Started familiarizing myself with the cloud image builder and successfully built my own Trixie RPi image using it. Will start making changes to the image builder and to raspi-firmware as needed in the near future.

=== Backport deprecation notice and release upgrade script changes to Bookworm ===
Date: 2026-03-12

Fixed the legacy-dist repository (its bookworm branch had fallen behind the actual contents of the archive), and backported needed changes to it. Tested release upgrades of Whonix-Gateway, Whonix-Workstation, and Kicksecure using the new code, all upgrades succeeded.

=== Report /tmp/user/1000 ownership bug to Qubes ===
Date: 2026-03-12

Created a bug report on the Qubes issue tracker since we now know this is almost certainly an upstream Qubes issue.

== 2026-03-11 ==
=== RPi + GRUB + UEFI upstreaming work #1 ===
Date: 2026-03-11

Ran into boot issues with the Debian cloud image for the Raspberry Pi, and attempted to debug why the image was not working. After much fiddling, it turned out to be the fault of the MBR partition table on the image, which Patrick discovered ultimately points to an issue with start.elf. Will be filing a bug and attempting to get it solved upstream.

=== Finish initial dist-installer-cli fixes for Ubuntu + Secure Boot ===
Date: 2026-03-11

Fixed Ubuntu support in general by switching to Oracle's VirtualBox package, also fixed Secure Boot issues to some degree by moving the Secure Boot key enrollment checks earlier. Further work should be done to automate the process of setting up Secure Boot keys if necessary.

=== Attempt to reproduce /tmp/user/1000 ownership issue with graphical KVM machine ===
Date: 2026-03-11

Installed a GUI on the KVM machine running the reproduction test and attempted to reproduce the issue there. So far, this has not been successful, so this is most likely a Qubes bug.

== 2026-03-10 ==
=== Rebase grub-pc + grub-efi co-installability patch ===
Date: 2026-03-10

Rebased the changes on top of the most recent code. Still awaiting review.

=== Make autologinchange more robust ===
Date: 2026-03-10

Investigated how SDDM and LightDM handle settings, and made it so that autologinchange could more reliably detect users that had autologin already enabled and more reliably disable autologin for those users.

=== Report /tmp/user/1000 ownership issue details to Qubes, attempt to reproduce with KVM ===
Date: 2026-03-10

Shared full reproduction steps with Marek on Matrix. Still testing things before filing a Qubes OS bug report since this may be a bug in upstream Debian.

=== Review, discuss Vulnerability Disclosure Policy ===
Date: 2026-03-10

Made some wording changes and suggested a zero-day disclosure policy for in-the-wild bugs directly affecting Kicksecure and Whonix. Discussed some mechanisms for proving when we created a vulnerability writeup using OpenTimestamps or other mechanisms.

== 2026-03-09 ==
=== Successfully reproduce Qubes /tmp/user/1000 ownership issue on debian-13-xfce ===
Date: 2026-03-09

I tried installing libpam-tmpdir in debian-13-xfce, then used a loop to reproduce the ownership bug. I was able to reproduce the issue in both Whonix and vanilla Debian.

=== Finish initial iteration of org.freedesktop.FileManager1 shim ===
Date: 2026-03-09

The shim is working for the most part, but a Qubes bug related to the systemd user manager's environment prevents it from being usable as-is yet. This will need fixed first.

=== WAITING ON cleanup ===
Date: 2026-03-09

Went through all of the WAITING ON tasks, moving them to their appropriate locations if they needed to be moved and following up on some stalled tasks.

=== sdwdate-gui-client 100% CPU usage bug reproduction attempt #2 ===
Date: 2026-03-09

saka414 provided more detailed reproduction steps for the issue, which I followed. I was unfortunately not able to reproduce the issue following the provided steps.

== 2026-03-08 ==
=== Start implementing org.freedesktop.FileManager1 shim ===
Date: 2026-03-08

This should fix the "Open in folder" button in Tor Browser under Qubes once finished. A lot of the code is done, but needs reorganized and thoroughly tested.

=== Briefly investigate dist-installer-cli functionality on Ubuntu with Secure Boot ===
Date: 2026-03-08

Was unable to install VirtualBox and a Kicksecure VM this way because of a combination of a problematic debconf prompt and a bug in the VirtualBox package for Ubuntu. This will need further work to fix (among other things, we should use Oracle's repo unconditionally because of the unreliability of other repos).

=== Rewrite one-time-popup, add passive notification support ===
Date: 2026-03-08

Originally I was just going to add passive notification support to the existing one-time-popup code, but after looking at the code, I noticed some design issues and bugs that I wanted to fix. Rewrote one-time-popup to resolve the design problems, fixed the bugs I saw, and added passive popup support.

=== Verify sdwdate-gui can be safely turned off on the server side ===
Date: 2026-03-08

saka414 on Matrix had been encountering some bugs with sdwdate-gui consuming 100% of all available CPU on the client VM when the server VM had sdwdate-gui disabled. Was not able to trivially reproduce the issue, however saka414 has since given more info about how to reproduce the issue, so this needs further investigation.

=== Create tracker for security bug reports ===
Date: 2026-03-08

As discussed. Details of reported issues are kept private.

== 2026-03-07 ==
=== Finish enhancements on privleap test suite, attempt to add CI support ===
Date: 2026-03-07

Added a couple of tests to exercise a couple of edge cases that weren't being properly covered previously. Tried to add Github Actions CI; unfortunately this was not successful because running autopkgtest under Github Actions ended up being impossible due to some issues writing to <code>/dev/stderr</code> within the Github Actions environment.

== 2026-03-06 ==
=== Get privleap test suite to pass, augment it to properly test new multi-argument support ===
Date: 2026-03-06

There are a few changes I'd still like to make to the test suite to make test coverage better, but for the most part this appears done and should be ready to add to trixie-developers in the near future.

== 2026-03-05 ==
=== Make substantial progress on privleap multiple action authorization checks ===
Date: 2026-03-05

Fixed some mistakes in the refactoring, tried to run the test suite but ran into failures.

=== Age API wiki page review #2 ===
Date: 2026-03-05

Reviewed, made some more changes and did more research.

== 2026-03-04 ==
=== Begin adding to privleap the ability to check permissions for multiple actions at once ===
Date: 2026-03-04

This necessitated amending the protocol design to allow commands with multiple arguments. The library and client side of this is mostly implemented, but needs polished, and the server side needs implemented still. Tests will need to change significantly.

=== Continued discussion of age-api concerns ===
Date: 2026-03-04

Discussing upstream about the intended implementation, discussing with Patrick and on the mailing lists.

=== Age API wiki page review #1 ===
Date: 2026-03-04

Reviewed, made minor changes to the [[age-api]] page.

== 2026-03-03 ==
=== Age API spec draft creation and review ===
Date: 2026-03-03

Finished drafting a new spec. Reviewed and discussed with Patrick, this was intended to be sent in the near future but after drafting it we discovered that there is already an xdg-desktop-portal implementation in progress that is a more likely candidate for "where things will go next".

== 2026-03-02 ==
=== More age-api research, begin drafting a more concrete spec ===
Date: 2026-03-02

Replied to some more emails, started drafting a better spec proposal. Will need fleshed out further.

=== Add enhanced multithreading tests to privleap test suite ===
Date: 2026-03-02

After running into a number of multithreading-related bugs, I decided we needed an additional test specifically for multithreading to try to provoke deadlocks and crashes. Added the tests and got them to work as expected.

=== Research, discuss remount-secure implementation techniques ===
Date: 2026-03-02

After discussion with Patrick, we likely have a way to implement remount-secure that should work reasonably well and be intuitive to use (namely the remount-secure daemon concept).

=== Finish review, fixing of stdisplay wiki page ===
Date: 2026-03-02

Figured out why using <code>TERM=dumb</code> was not causing ANSI color codes to be stripped, reintroduced the <code>TERM=dumb</code> method of disabling color to the wiki page and added a line of code to the example that fixed it.

== 2026-03-01 ==
=== Review untrusted data and stdisplay wiki pages ===
Date: 2026-03-01

Discovered a broken feature in stprint that needs fixed in the process (namely the <code>TERM</code> environment variable handling).

=== privleap robustness enhancements #5 ===
Date: 2026-03-01

Hopefully fixed the deadlock bug, got comm session termination to work correctly. The test suite needs to be able to exercise the multithreading code more vigorously.

=== Age verification API discussion and preliminary design ===
Date: 2026-03-01

Due to new laws that are either passed or being worked on in California and Colorado, we are looking into how to implement a so-called "age verification" API that does not compromise the user's privacy or anonymity. Discussed with Patrick, submitted an initial design draft to several open-source development and legal mailing lists and was active in the ensuing discussion.

== 2026-02-28 ==
=== privleap robustness enhancements #4 ===
Date: 2026-02-28

Tried to implement comm session termination when a comm socket is destroyed. Ran into a very annoying deadlock bug that hampered initial attempts to test this, how things get deadlocked is unclear.

== 2026-02-27 ==
=== privleap robustness enhancements #3 ===
Date: 2026-02-27

Found the cause of the performance regression and fixed it. Also fixed an issue where new persistent users would not have comm sockets created for them after a config reload.

=== Add packages from the efi_weak_recommended_packages_list to the Kicksecure ISO ===
Date: 2026-02-27

Makes consistency a bit better and may help during future work on Secure Boot.

== 2026-02-26 ==
=== privleap robustness enhancements #2 ===
Date: 2026-02-26

Mostly finished testing the new split-out control thread. It mostly behaves well, but there is a performance regression causing a test to fail. Needs further investigation.

=== Update anon-timezone PR for Qubes ===
Date: 2026-02-26

Made a change Ben requested (avoiding comparing against the string "1" and instead treating <code>untrusted_value</code> as a boolean-like value), updated the tests to work with the change.

== 2026-02-25 ==
=== privleap robustness enhancements #1 ===
Date: 2026-02-25

Started looking at privleapd's multithreading code, investigating possible issues with thread safety and how to improve efficiency without causing problems. Split the control connection handler into its own thread, however I have not yet tested the new code since it needs a bit more work.

=== Create qubes-core-admin PR to allow VMs to request the anon-timezone feature ===
Date: 2026-02-25

Test suite passes, Ben Grande has requested a minor change though.

=== Research Catfish thumbnail previews ===
Date: 2026-02-25

Failed to find a way to reliably disable thumbnail generation in a way better than what already exists. Documented my findings on the todo wiki page.

=== Revert fwupd-signed dummy package introduction to unbreak build ===
Date: 2026-02-25

With genmkfile working, the only issue standing in the way of a working ISO build was to get rid of dummy-dependency-fwupd-signed. Did this, and got a Kicksecure 18 ISO built in a Kicksecure 18 AppVM under Qubes R4.3.

== 2026-02-24 ==
=== sha256sum research ===
Date: 2026-02-24

As discussed.

=== Disable time-related services in Qubes VMs that do not have a NetVM ===
Date: 2026-02-24

This should fix TOTP in vault VMs hopefully, and prevent strange sdwdate-gui behavior when a vault VM is booted.

=== Make QTerminal window smaller by default ===
Date: 2026-02-24

The window should now be the exact right size to provide an 80x24 character display on new installations.

=== Reproduce, report qps crash on systems with hyperthreading disabled ===
Date: 2026-02-24

Was able to reproduce the crash reported by a forum user. Reported upstream. Will report to Debian once this is dealt with upstream.

=== Create PRs to tag Kicksecure and Whonix AppVMs appropriately during template changes ===
Date: 2026-02-24

Created one PR each for qubes-core-admin-addon-whonix and qubes-core-admin-addon-kicksecure. Awaiting review and merge.

=== Finish fixing genmkfile and mixed-architecture packages ===
Date: 2026-02-24

Found the last issue that was tripping up genmkfile. It can now handle source packages that are built into binaries for multiple architectures. Also discovered that our new dummy packages for code simplification (for things like fwupd-signed) did not quite work as intended and will need to be partially reverted.

=== More Sequoia-PGP tb-updater code review ===
Date: 2026-02-24

Patrick made further changes after my initial review. Reviewed those changes, did some minor cleanup on the code.

== 2026-02-23 ==
=== Work on making genmkfile able to handle mixed-architecture packages ===
Date: 2026-02-23

genmkfile was crashing trying to work with packages that output both "all" and "amd64" architecture packages. Did most of the work needed to fix this (the work done here turned out to not be quite sufficient, but was close).

=== Review new OpenPGP Sequoia code in tb-updater ===
Date: 2026-02-23

Looked at some of the Sequoia-PGP code Patrick added, made minor changes (mostly comments).

== 2026-02-22 ==
=== systemcheck and metapackage updates to simplify code, detect issues with leaprun actions and unexpected persistent dirs on Qubes ===
Date: 2026-02-22

Added extra dummy packages to kicksecure-meta-packages to allow simplifying code that would conditionally install packages in other situations. Added functionality to systemcheck to detect if /etc, /usr, or /var are persistent in Qubes AppVMs (they should not be), also wrote code that checks if leaprun actions are able to be run before running them (this is too slow to be practical and isn't working quite as intended yet). 

=== Finish testing and polish of Whonix-Starter ===
Date: 2026-02-22

Test plan passes, both Linux and Windows variants appear to work well at this point. Submitted for review.

== 2026-02-21 ==
=== Further polish on Whonix-Starter UX, refine green turtle workaround ===
Date: 2026-02-21

Improved error popups, adding more detailed debugging information and the ability to immediately open wiki pages with troubleshooting info. Improved the UX when starting either the Gateway or Workstation when the other one is already running. Made the behavior of the "Stop Whonix" button less confusing when no Whonix VMs are running. Tried and failed to add a green turtle/blue microchip icon button to the application (issues with the .ICO file format led to me abandoning this, though it should be theoretically possible to do), and elected to make the status bar double-clickable to show information about VM speed instead. Fixed Windows build issues. Greatly lengthened the pause between launching the gateway and launching the workstation (10 seconds was too short and both VMs stalled, 15 seconds was too short and the gateway only stalled, 20 seconds will hopefully be enough to allow both to start even on less-than-lightning-fast hardware). Didn't quite get to running the test plan yet.

== 2026-02-20 ==
=== More Whonix-Starter development, create test plan ===
Date: 2026-02-20

Whonix-Starter now has a theoretically good workaround for the green turtle issue. A test plan has been created, if the application passes then it should be ready for release to other users.

=== Finish debugging Sequoia + split-gpg2 issues, file feature request ===
Date: 2026-02-20

Finally pinned down the issue; gpg-sq is missing the <code>--export-secret-subkeys</code> option. Filed a feature request with Sequoia, and suggested in the Qubes bug tracker that porting to using sq itself might be a good option.

== 2026-02-19 ==
=== Continued Whonix-Starter development, finish getting external processes working ===
Date: 2026-02-19

Finally found a way of executing external processes from within both Linux and Windows that works efficiently and quickly. Now trying to work around the green turtle issue and improve the UX.

=== Don't abort systemcheck when run on an offline AppVM, sequoia debugging ===
Date: 2026-02-19

The <code>check_qubes.bsh</code> script was making systemcheck exit entirely when the lack of a network connection was detected. Fixed this, and also downgraded some "no network found" errors to warnings.

Did some debugging with Patrick to figure out why gpg-sq and split-gpg2 were not working well together. Couldn't figure out exactly what was wrong, but got closer. More research to be done soon.

== 2026-02-18 ==
=== Debug starting external processes in Free Pascal for Whonix-Starter development ===
Date: 2026-02-18

It appears I have run into two different bugs in Free Pascal, one that makes capturing the output of external commands with one method difficult under Windows, and one that makes another method extremely slow under Linux. Tried to debug why this was happening, came up with the idea of using low-level I/O operations on stdio handles to work around the issue.

=== Set up Qubes development rig ===
Date: 2026-02-18

Set up a new mini PC with Qubes OS for Kicksecure development. Made a new PGP key, experimented with using Sequoia-PGP and potentially discovered a bug in it.

== 2026-02-17 ==
=== Whonix-Starter debugging on Windows, try creating an NSIS installer ===
Date: 2026-02-17

Whonix-Starter ended up not working correctly on Windows due to what appears to be a Free Pascal bug. Did some debugging and attempted workarounds. Also created an NSIS installer script for the application, this seems to work well for both silent and manual installation.

== 2026-02-16 ==
=== Initial rewrite of Whonix-Starter ===
Date: 2026-02-16

Wrote Whonix-Starter from scratch, getting it to work on Linux first. Seems to be working well to begin with.

=== Regular tasks (forums, documenting work) ===
Date: 2026-02-16

Had an unusually large amount of posts to sort through on the forums.

== 2026-02-15 ==
=== Debug, fix Whonix 17 to Whonix 18 update failure caused by policy-rc.d, fix kloak failing to start after upgrade ===
Date: 2026-02-15

Whonix 17 upgrades were failing because the policy-rc.d file originally shipped by usability-misc was disappearing mid-way through the upgrade procedure. Determined why this was causing problems, and created a workaround.

Also reproduced a bug where kloak failed to start after an upgrade, determined what was causing the issue, and created a fix for it.

=== Rewrite dm-tor-update-repository to use an apt + chroot mechanism ===
Date: 2026-02-15

When I originally wrote dm-tor-update-repository, I wrote it in Python and made it handle the process of validating package metadata manually. After discussion with Patrick, we decided this was non-ideal and we would rather offload the job to apt, even though this would result in the code being somewhat more brittle and requiring much more network I/O. Reimplemented dm-tor-update-repository in Bash, using a cowbuilder chroot and apt to download and verify the Tor package metadata.

=== Investigate fwupd failure messages likely related to bdev_allow_write_mounted=0 ===
Date: 2026-02-15

Attempted to figure out why fwupd was attempting to open disk partition devices. Posted my findings in the Github issue Patrick pinged me on when asking this question.

=== Add sysmaint and user accounts to the privleap group by default in new builds ===
Date: 2026-02-15

This will mitigate a minor warning in systemcheck.

=== Finish making set-keyboard-layout.sh and kloak work together better ===
Date: 2026-02-15

set-keyboard-layout.sh can now tell the user how to restart kloak if it's necessary to, and can restart kloak itself if setting the system-wide keyboard layout.

== 2026-02-14 ==
=== Debug sequoia-pgp porting, Whonix 17-to-18 upgrade issues, finish fixing set-keyboard-layout.sh and kloak ===
Date: 2026-02-14

Found some bugs related to gpg-from-sq and discussed them with Patrick. Ran into an issue upgrading Whonix 17 to Whonix 18 and discussed how to debug and if it was worth fixing. Got set-keyboard-layout.sh to restart kloak itself when safe, and to tell the user how to restart it otherwise.

== 2026-02-13 ==
=== Fix output bug in set-keyboard-layout.sh, start work on alerting the user when kloak needs a restart ===
Date: 2026-02-13

Some of the output from set-keyboard-layout.sh was confusing because of the order it was presented in. Swapped things around to resolve this. Also added code that detects when kloak needs restarted after a keyboard layout change, and informs the user of this.

=== Finish initial merged TCP + ACW review ===
Date: 2026-02-13

Final conclusion, the code is not malicious but needs further work before it can be merged (it has some bugs and shortcomings that make it less than ideal for release yet).

=== Study and silence more systemcheck messages ===
Date: 2026-02-13

Silenced several more messages that Patrick discovered. Further investigation needs to be done on one message related to VirtualBox guest additions.

== 2026-02-12 ==
=== Start implementing the redesigned Whonix-Starter ===
Date: 2026-02-12

Currently taking a "rebuild from scratch using the previous version as a template" approach, to avoid potential pitfalls with the original (such as possible reentrancy issues with the external command executor).

=== Fix systemcheck critical warnings checker and make it check the previous boot's logs ===
Date: 2026-02-12

The critical warnings checker was broken because it was using a fixed-strings match mode with an extended regexp. Fixed the issue, then added the ability to check the previous boot's logs for critical errors. Tested, appears to work on a KVM Kicksecure installation.

=== Investigate new systemcheck warnings from Patrick ===
Date: 2026-02-12

Researched all of them, silenced some of them, shared ideas on how to improve privleap inspired by one of the new warnings that was showing up.

=== Stop installing kloak on Whonix-Workstation CLI ===
Date: 2026-02-12

Fixed the metapackages so that kloak won't be automatically installed on CLI VMs anymore. This should allow it to be autoremoved on existing systems in the future.

=== Merged TCP + ACW review #3 ===
Date: 2026-02-11

Continuing to make notes as I go. At this point I'm most of the way through Tor Control Panel itself.

== 2026-02-11 ==
=== Redesign user interface for Whonix-Starter ===
Date: 2026-02-11

Learned how to create layout-style UIs in Lazarus, created a UI mockup and shared it with Patrick, discussed the intended end goal and how to best acheive it.

=== Merged TCP + ACW review #2 ===
Date: 2026-02-11

Continuing to make notes as I go.

=== Fix systemcheck --verbose --leak-tests failure on first boot ===
Date: 2026-02-11

systemcheck was erroring out if run as <code>systemcheck --verbose --leak-tests</code> on the first boot, since the previous boot logs didn't exist and the mechanism to silence that warning didn't work right. Fixed the issue.

== 2026-02-10 ==
=== Start review of merged Tor Control Panel + Anon Connection Wizard ===
Date: 2026-02-10

Made several notes as I went. Noted some issues, but they all look relatively minor.

=== Mostly finish getting Windows installed for Whonix-Starter development ===
Date: 2026-02-10

Ended up with some issues with the EFI system partition, but those were able to be worked out.

== 2026-02-09 ==
=== Work on re-creating a Windows environment for Whonix-Starter development, discuss signing ===
Date: 2026-02-09

I didn't want to run possibly untrusted software on my system, so I tried to make a Windows installation USB from an ISO file without using software outside of the Debian archives. Discovered multiple ways that didn't work, and got most of the way to a working solution when doing this. Also discussed with Patrick how to get Whonix's Windows applications signed.

=== Fix systemcheck reporting a non-login environment on CLI installations ===
Date: 2026-02-09

This turned out to be because a file in /etc/profile.d wasn't symlinked into /etc/zprofile.d properly. Added the missing symlink.

=== Silence zsh setup wizard for CLI installations ===
Date: 2026-02-09

Added a do-nothing function to zshenv to make its setup wizard do nothing.

=== Research how to disable 3d acceleration with the Nouveau driver ===
Date: 2026-02-09

Originally, I was trying to see if I could get a simple driver other than Nouveau to work on an NVIDIA-only system, but this proved to be impractical due to the generally unusable nature of a raw framebuffer at low resolutions and the fact that wlroots lacked an fbdev backend. After abandoning that, I learned that it was possible to disable 3d acceleration with Nouveau, and suggested to Nurmagoz that they try that to see if it fixed instability issues. It did indeed do what was needed.

=== Add checks for "fault", "killed", and "segfault" to systemcheck's log checker ===
Date: 2026-02-09

This should detect issues like labwc crashing because of Nouveau.

=== Make many -cli metapackages directly installable ===
Date: 2026-02-09

It will now be possible to install Kicksecure's non-LXQt, non-server specific bits by using a -cli metapackage directly. Previously, these metapackages were not designed for direct installation.

== 2026-02-08 ==
=== Document the places where our core GRUB config generation code differs from Debian ===
Date: 2026-02-08

Added comments where appropriate so that we can easily merge future changes to /etc/grub.d into our configuration in the future.

=== Add kanshi configuration helpers, document how to use them ===
Date: 2026-02-08

wdisplays is problematic because its display configuration is undone by a reboot. kanshi is difficult to use, but allows persistent configuration. Added documentation for how to use it, some buttons to make it easier to use, and a systemd unit to automatically start it.

=== Get extrepo CI to pass so onion link additions can be merged ===
Date: 2026-02-08

The CI code will now retry connections up to three times, providing some level of resiliency against the natural flakiness of the Tor network.

=== Finish systemcheck journal warning research and silencing ===
Date: 2026-02-08

Finished looking at the journal warnings presented by many common scenarios, silencing them and documenting why.

== 2026-02-07 ==
=== Continue systemcheck journal warning research and silencing ===
Date: 2026-02-07

Found a large number of additional journal warnings to silence, researched their root causes, filed bug reports where necessary. Very nearly done with looking through these.

== 2026-02-06 ==
=== Silence more systemcheck journal warnings, add the ability to scan the previous boot's journal ===
Date: 2026-02-06

systemcheck is now able to scan the journal logs from the previous boot. This should help make technical support easier. It also catches errors that only show up during shutdown. There are still many more pseudo-errors to be silenced.

=== Attempt to reproduce sysmaint session crash on an NVIDIA system ===
Date: 2026-02-06

This was a follow-up to the sysmaint session crash issue reported by Nurmagoz. I was not able to reproduce the issue with either Nouveau or proprietary drivers on my 4070 system. Nurmagoz was able to fix the issue on their end by installing the proprietary drivers and suggested some ways of preventing the issue for other users.

=== File new bug report for Qubes VM tagging limitations ===
Date: 2026-02-06

Filed a bug report about the issue where qubesd needs to be restarted to ensure Whonix and Kicksecure VMs are properly tagged in some instances.

== 2026-02-05 ==
=== Start work on fixing systemcheck journal scanning ===
Date: 2026-02-05

Discovered that the log checker in systemcheck was completely broken, fixed it, and started work on fixing new journal warnings.

=== Ensure our /etc/grub.d files are up-to-date ===
Date: 2026-02-05

Checked our files against those currently present in Debian Trixie. All of the differences look intentional, it doesn't look like there's anything new to carry over.

=== Finish investigation into Debian's livepatch enablement policy ===
Date: 2026-02-05

Right now livepatch is being intentionally enabled, and there are other projects larger than tirdad that depend on it (TuxCare's KernelCare). It has always been disabled by default upstream AFAICT, Debian's kernel configuration enables it. Therefore this is likely going to still be available into the future.

=== Disable num lock by default ===
Date: 2026-02-05

This prevents issues on smaller laptops with a numpad integrated into the main area of the keyboard. (Previously we were always enabling numlock due to the labwc configuration we took from lxqt-wayland-session.)

=== Allow user-sysmaint-split to be removed without purging under Qubes OS ===
Date: 2026-02-05

Removing user-sysmaint-split without purging it previously resulted in template bootup issues because the configuration file that created the boot mode entry was being left behind, thus making dom0 think the VM could still be booted in sysmaint mode when this wasn't actually possible. Changed the config file into a symlink so that simply removing user-sysmaint-split is now enough.

=== Make tb-updater's postinst not crash an upgrade if sandbox-update-torbrowser is already running ===
Date: 2026-02-05

The postinst will now warn if tb-updater fails, but will not crash the upgrade, unless running in a chroot and configured to fail closed.

=== Test sysmaint session crash reported by Nurmagoz ===
Date: 2026-02-05

Nurmagoz reported that the sysmaint session would crash when a USB hard drive was plugged in. After testing, I was unable to reproduce this. Further debugging revealed that the compositor was crashing in a graphics driver library. The issue may be NVIDIA-related.

== 2026-02-04 ==
=== Make user-sysmaint-split removal on Qubes OS safe without using --purge ===
Date: 2026-02-04

Right now in order to safely remove user-sysmaint-split from Qubes OS, one must use the <code>--purge</code> option to dummy-dependency or apt-get. Otherwise the boot mode configuration file in /etc/qubes/post-install.d is left on-disk, the sysmaint boot mode is left present incorrctly, and the VM will fail to boot if a user tries to boot it in this mode. Made a fix for this locally, still need to test it.

=== Finish testing helper-scripts refactoring and polish of gsudo-{wl,x} ===
Date: 2026-02-04

The refactoring and gsudo-* work is now done and appears to work as expected.

== 2026-02-02 ==
=== More work on helper-scripts refactor and gsudo-{wl,x} ===
Date: 2026-02-02

Tested and fixed several issues with the refactoring code, reviewed change requests on gsudo-* from Patrick, established what still needs changed, and what is and isn't working. A small amount of this work was lost cleaning up after a supply-chain attack I was affected by (refactoring on tb-updater); this will be very easy and fast to redo.

== 2026-02-01 ==
=== gsudo-{wl,x} fixes, refactoring of helper-scripts, emergency bugfixes for tb-updater ===
Date: 2026-02-01

Fixed the <code>$DISPLAY</code> environment variable not being set in sysmaint sessions. Started refactoring the way script source checks work throughout the entire Kicksecure and Whonix codebases. Helped Patrick fix a permissions bug in tb-updater, and also fixed a bug with detecting previously downloaded versions of Tor Browser when sandbox-update-torbrowser is in use.

=== Fix tb-updater shortcomings on Kicksecure ===
Date: 2026-02-01

Added a configuration option to tb-updater for allowing it to run in sysmaint sessions, and made the error message about startup suppression in sysmaint sessions show up faster and be more concise. Also fixed the <code>--onion</code> option under Kicksecure (it worked on Whonix, but was failing on Kicksecure).

=== Diagnose systemcheck HTTPS error, help on forums ===
Date: 2026-02-01

Ben Grande reported some time ago that systemcheck failed with a strange HTTPS error. Looked at the code with Patrick and concluded this was likely a transient issue with check.torproject.org. Also helped out on the forums.

== 2026-01-30 ==
=== Investigate handling of some D-Bus methods ===
Date: 2026-01-30

Shared results with Patrick.

== 2026-01-29 ==
=== Start improving the robustness of autologinchange ===
Date: 2026-01-29

Mostly worked on making the removal of autologin configuration from config files more robust. Mostly got the lightdm portion of the code working well, though I ran into a regex issue that still needs fixed.

=== Researched adding keyboard LED control to backlight-tool-dist ===
Date: 2026-01-29

Made some notes locally about what needed to be done to add keyboard LED support to the tool, and what the intended UX should likely be.

=== Fix piuparts failure on GRUB de-conflict PR ===
Date: 2026-01-29

Discovered that the source of the piuparts failure was that the new grub-default-config package was being installed by itself with none of the "main" packages that installed a bootloader. Fixed by adding a dependency on any one of the "main" GRUB packages.

=== File extrepo policy override feature request and more enhancements to extrepo-data ===
Date: 2026-01-29

Filed a feature request for making it possible to override extrepo's policy configuration by using command-line options. Also submitted enhancements to extrepo-data that make our onion repos available, refresh the signing key again, and fix some issues with the repo template file.

=== Fix firmware-nonfreedom-network dependency in main package kicksecure-qubes-cli ===
Date: 2026-01-29

Removed the dependency so that we would not have a package in the main repo depending on a package in the non-free repo. Adjusted the Kicksecure Qubes template so it would install the firmware-nonfreedom-network during the template build.

== 2026-01-28 ==
=== More research into U-Boot + GRUB on Raspberry Pi ===
Date: 2026-01-28

Looked more closely at some MRs and an article by another Debian contributor (Kurva) who was working on Debian-on-RPi enhancements prior to our initial work on GRUB and UEFI on the Raspberry Pi. Concluded that attempting to inject GRUB support into the existing plans could potentially cause undue conflict and that it would be better to help the existing initiative succeed, then build on top of it.

=== Start investigating piuparts CI failure on GRUB de-conflict MR ===
Date: 2026-01-28

Briefly looked at the piuparts failure log, could not initially determine what was going wrong and asked for help in #debian-devel.

=== Review new wlr-resize-watcher documentation and code ===
Date: 2026-01-28

Patrick enhanced the wlr-resize-watcher code and documented it on the wiki. Read through the code enhancements and documentation, made some minor changes to the wiki page.

=== Investigate if dummy-dependency-mta could be removed, document how to remove dummy-dependency-* packages ===
Date: 2026-01-28

After some testing, I found that the reason dummy-dependency-mta had to exist was because Kicksecure 17 came with gpg-wks-server preinstalled, and while that package did not depend on a mail server in Bookworm, it does in Trixie. Documented how to remove it, and some other dummy-dependency packages as well.

=== Display a warning in systemcheck if started outside of a login environment ===
Date: 2026-01-28

Made systemcheck look for an environment variable that is always set when run in a login environment, and warn the user that they should log in as the user they're running systemcheck as if that variable is not set.

=== Manually test DKMS MOK generation suppression PR ===
Date: 2026-01-28

Ensured that when installing a DKMS module in a chroot, a MOK was not generated due to a chroot being used, and that this could be overridden by configuration.

=== Further polish on gsudo-wl, gsudo-x ===
Date: 2026-01-28

Switched from using <code>mkdir</code> to <code>install</code> in a couple of places, and made the cleanup routine check that variables are set before attempting to use them.

== 2026-01-26 ==
=== Create PR for suppressing DKMS MOK generation in chroots ===
Date: 2026-01-26

Wrote the code necessary to prevent DKMS MOK generation in chroots, along with accompanying tests. The test suite passes, and upstream has merged it, but this still needs a bit more manual testing before I'm 100% happy with it.

=== Test, polish sandbox-update-torbrowser after fixes and lockfile addition ===
Date: 2026-01-26

Made sure sandbox-update-torbrowser still worked on Qubes OS and when building VM images. Made a couple more changes in the process.

== 2026-01-25 ==
=== Add single-instance locking to sandbox-update-torbrowser, fix cgroup handling issues ===
Date: 2026-01-25

Used the helper-scripts <code>lockfile.sh</code> mechanism for locking. Made changes to the cgroup launch script in helper-scripts to allow using an existing cgroup (useful for cleaning up msgprogress instances), used those changes in sandbox-update-torbrowser, fixed the cgroup cleanup routine (which turned out to be unreliable due to <code>cgroup.kill</code> being asynchronous), and fixed an issue where msgdispatcher wasn't being run in the correct manner.

=== Add warning to systemcheck when running outside of a login environment ===
Date: 2026-01-25

When systemcheck is run outside of a "login environment" (i.e. <code>sudo -u user2 systemcheck</code>), it malfunctions rather badly, due to issues such as missing environment variables. This isn't dangerous, but it can be very confusing to a user. Added a check that detects when systemcheck is run outside of a login environment, and warns the user that many tests are likely to fail when run in a non-login environment.

=== Write gsudo-wl and gsudo-x applications ===
Date: 2026-01-25

<code>gsudo-wl</code> launches GUI applications as root under Wayland, while <code>gsudo-x</code> launches GUI applications as root under X. Tested these scripts, reviewed them with ChatGPT, and pushed them.

=== Finish getting extrepo merge request accepted ===
Date: 2026-01-25

During the server distribution morphing work, I filed a PR against extrepo-data that added the trixie repository for Kicksecure and Whonix to the list of available repos. Some changes were needed (updating a signing key and dropping Bullseye from Kicksecure's repo list); these changes were made, and the MR has been merged.

=== Fix corrupted autologin configuration bug in helper-scripts ===
Date: 2026-01-25

Recently, a bug in autologinchange was discovered, which resulted in some accounts that had autologin enabled not being recognized as such. Fixing this bug uncovered another bug that would result in autologin configuration being corrupted while disabling autologin for the <code>sysmaint</code> account in "raw" mode. Found and fixed the issue.

== 2026-01-24 ==
=== Fix Qubes proxy handling in browser-choice ===
Date: 2026-01-24

browser-choice was exporting the correct proxy environment variables, but failed to take into account that <code>pkexec</code> would strip those variables when executing many of the installation scripts. Changed the way in which proxy environment variables are passed to the install scripts so that it would work despite pkexec's environment sanitization.

=== Implement fixes for most server distribution morphing issues ===
Date: 2026-01-24

Updated documentation and made several code changes so that distribution morphing on a server would go more smoothly. Some documentation work is still needed.

=== Discuss plans for gsudo-wl and gsudo-x development ===
Date: 2026-01-24

Clarified with Patrick the intended goals of the <code>gsudo-wl</code> and <code>gsudo-x</code> applications, in preparation for writing the tools in the near future.

== 2026-01-23 ==
=== Research how to make server distribution morphing more reliable ===
Date: 2026-01-23

Looked at several issues with server distribution morphing encountered by Patrick. Documented suggestions for fixing each of them, will be working on implementation soon.

=== Investigate dummy-dependency-mta causing problems with Postfix installation ===
Date: 2026-01-23

Started discussing possible solutions with Patrick.

=== Prevent grep lightdm error message when using autologinchange with /etc/lightdm not present ===
Date: 2026-01-23

<code>autologinchange</code> assumed that <code>/etc/lightdm</code> would always exist, and used <code>grep</code> in a way that caused it to show an error if it didn't exist. Fixed the issue, and also fixed a problem where autologin-enabled users in greetd weren't taken into account.

=== Fix confusing message in DKMS module rebuilder ===
Date: 2026-01-23

Changed the message for when there are no DKMS modules to rebuild, so that it explicitly says that rather than saying "everything was built".

=== Finish initial review of clipboard sharing workaround script ===
Date: 2026-01-23

Did not see anything malicious in the code. Posted several notes on the code, design, and UX. Ran the script in both a Kicksecure 18 VirtualBox VM and my host system after verifying it was safe, and confirmed that it works to enable clipboard sharing between the host and guest.

== 2026-01-22 ==
=== Begin reviewing clipboard sharing workaround script from tylereaston ===
Date: 2026-01-22

Reply is currently saved as a draft in Discourse and as a text document. Will continue reading through the code and will test it before sending reply.

=== Refactor MOK cleanup code ===
Date: 2026-01-22

Patrick requested several changes to the MOK cleanup code, mainly to make it so that systemcheck wouldn't make changes to the system itself. Made and re-tested the requested changes.

== 2026-01-21 ==
=== Resume investigation of Raspberry Pi support in Debian, test flash-kernel PR ===
Date: 2026-01-21

Could not figure out how to get flash-kernel and U-Boot to work together initially. Sent a message to the RPi4 flash-kernel PR mentioning this. Further research will be needed to figure out how to get native Debian RPi images to use U-Boot + GRUB by default in a way upstream will accept.

=== Fix sdwdate-gui flooding dom0 journal when the server is disabled but the client is enabled ===
Date: 2026-01-21

If the server is disabled, qrexec will now communicate this back to the client VM and disable it also.

=== Discover, report issues with shim idea for fixing Tor Browser "open in folder" ===
Date: 2026-01-21

The shim I started writing yesterday ended up being impossible to implement in a secure fashion (due to PCManFM-Qt's behavior it would result in unavoidable TOCTOU vulnerabilities). Documented this on the forums, discussed other ways of fixing the bug with Patrick, and pinged Marek about the issue.

=== Make dist-base-files' Dracut configuration to override dracut-config-generic ===
Date: 2026-01-21

Renamed <code>30-dist-base-files.conf</code> to <code>60-dist-base-files.conf</code> so it would override dracut-config-generic.

== 2026-01-20 ==
=== Start writing shim to allow Tor Browser to launch pcmanfm-qt ===
Date: 2026-01-20

The only way for Tor Browser to be able to launch a file manager (like is necessary for the "Open containing folder" feature of the Downloads menu to work) is to use D-Bus. Unfortunately, PCManFM-Qt is not able to be D-Bus activated, and running it in daemon mode would require too much RAM, so we have decided to create a D-Bus activatable shim that will handle launching the file manager. Started writing this shim.

=== Debug /dev/disk/by-diskseq/2 stuck in "tentative" state ===
Date: 2026-01-20

Discovered that we were not building Dracut initramfs images in hostonly mode, which was causing the issue. Fixed the issue for new images, discussed ideas for fixing this for existing images with Patrick.

=== Reorganize code for browser-choice unprivileged browser installation on ISO ===
Date: 2026-01-20

Patrick asked if the privleap configuration file for allowing unprivileged browser installation could be moved to the browser-choice package. Moved it to a location where it wasn't active, and created a systemd unit for copying it to the privleap conf location during ISO startup.

== 2026-01-19 ==
=== Test, polish livecheck enhancements ===
Date: 2026-01-19

Found and fixed several bugs in the process.

=== Finish and test new MOK reset functionality ===
Date: 2026-01-19

Finished fleshing out the MOK reset features, tested them in a variety of situations. Everything seems to work.

== 2026-01-18 ==
=== Work on adding MOK reset functionality to helper-scripts and systemcheck ===
Date: 2026-01-18

Many older Kicksecure images have problematic embedded MOK keys. This functionality will automatically remove them from systems that can be safely fixed automatically, and help users fix the issue when manual intervention is required.

=== Add CLI mode, logging, and improved error handling to livecheck ===
Date: 2025-01-18

This should make it easier to debug issues with Livecheck in the future. Some rearchitecturing should be done to make it more intuitive to use the CLI mode.

=== tb-updater enhancement tests ===
Date: 2026-01-18

Tested tb-updater (both in usual use and with its new sandboxed mode) in multiple use cases, ensuring it still worked as expected and fixing a minor issue in the process.

== 2026-01-17 ==
=== tb-updater enhancements (part 6) ===
Date: 2026-01-17

Fixed image build issues, got substantially more of the testing done. Hopefully very close to being done with this task.

== 2026-01-16 ==
=== tb-updater enhancements (part 5) ===
Date: 2025-01-16

Further polish on the code, added some new components to allow running an application in a new cgroup so that msgcollector could be terminated by sandbox-update-torbrowser properly. Started comprehensive testing.

== 2026-01-15 ==
=== tb-updater enhancements (part 4) ===
Date: 2026-01-15

The work over the last few days has been to sandbox the update-torbrowser script for greater security. Found and fixed more bugs. Created a test plan and started working through it.

== 2026-01-14 ==
=== tb-updater enhancements (part 3) ===
Date: 2026-01-14

More research, discussion, initial implementation created for preliminary review.

== 2026-01-13 ==
=== Test Whonix autologin failure, DKMS build failure ===
Date: 2026-01-13

Could not reproduce autologin failures, suggested to a forum user that they check the logs for greetd.service. Also ensured that DKMS builds of tirdad were working as expected after a user reported they were failing on their machine.

=== tb-updater enhancements (part 2) ===
Date: 2026-01-13

More research, discussion.

== 2026-01-12 ==
=== tb-updater enhancements (part 1) ===
Date: 2026-01-12

Discussed some ways of improving tb-updater with Patrick, started work on this.

=== File Qubes OS bug report for keyboard layout switching under KDE ===
Date: 2026-01-12

One cannot switch keyboard layouts in AppVMs using a keyboard shortcut when using KDE in dom0. Filed a bug report about this against Qubes OS.

=== Added usbguard-dbus.service to user-sysmaint-split default services ===
Date: 2026-01-12

Also notified the forums of this change. Did routine tasks like catching up on the forums at the same time.

== 2026-01-11 ==
=== Create initial draft of sandboxed application manager requirements and design ===
Date: 2026-01-11

After quite a bit of discussion and research, I wrote down what we need out of a sandbox manager, how it could work, and what the limitations of existing solutions were. Shared on the forums. Further discussion will be necessary before attempting to implement this.

=== Debug issue with multiple keyboard layouts in Whonix under Qubes OS ===
Date: 2026-01-11

Due to lxqt-session not being installed or running (by design) in Qubes-Whonix, multiple keyboard layouts weren't working in the expected manner. Discovered this, shared test and debugging results in the forums, and suggested some possible solutions.

=== Investigate tb-updater improvements ===
Date: 2026-01-11

Discussed with Patrick how to make tb-updater more robust, and created a list of things to change. Shared the list in chat.

=== Create new library for waiting for audio initialization, polish speaker volume fix ===
Date: 2026-01-11

Patrick asked me to break part of the speaker volume adjustment code into a new library to reduce code duplication. Did this, and fixed some other issues he pointed out with the initial implementation.

== 2026-01-10 ==
=== Added do-once mechanism for setting the speaker volume to 50% on the first login ===
Date: 2026-01-10

The earlier speaker volume adjustment had to be reverted, but a speaker volume implementation did end up being necessary since users were running into dangerously high default volume on physical hardware. Added code that sets the speaker volume to 50% on the first login and then never touches it again.

=== Fix battery icon being hidden on physical hardware ===
Date: 2026-01-10

A timeout wrapper added to the power management suppress script in vm-config-dist made the script always believe it was running under a VM. It was thus disabling power management unconditionally. Discovered why and fixed the issue.

=== Document how to disable usbguard ===
Date: 2026-01-10

A user reported that simply using <code>sudo systemctl disable usbguard.service</code> was not enough. Discovered why this didn't work; one needs to also disable <code>usbguard-dbus.service</code>. Documented this on the wiki (but failed to document turning it back on for re-enabling it, Patrick noticed and fixed it).

=== Debug qvm-tag migration issues for upgrades ===
Date: 2026-01-10

Verified that the automatic tagging mechanism for Whonix and Kicksecure VMs was working. Informed the user running into a problem about how to use it, we should document this better if we haven't already. (Later on, I ended up finding a bug in a particular automatic tagging edge case involving named disposables, and submitted a PR to Qubes to fix it.)

=== Investigate permission issues with tb-updater attempting to delete files from /var/cache/tb-binary ===
Date: 2026-01-10

Could not initially reproduce after multiple attempts. Requested more information.

== 2026-01-09 ==
=== Sandboxing technique research (part 2) ===
Date: 2026-01-09

Discussed apparmor.d, virtual machines, LXC, and other mechanisms with Patrick. Got a rough idea of what we're trying to acheive, started fleshing it out into a forums post (yet to be made public).

=== Second review of pre.sh and get_colors.bsh scripts ===
Date: 2026-01-09

Patrick made more changes to these scripts. Reviewed, looked good, made some minor adjustments.

== 2026-01-08 ==
=== Sandboxing technique research (part 1) ===
Date: 2026-01-08

Looked into 3d acceleration in VMs, Bubblewrap, Flatpak, and Landlock. Documented some rough thoughts on the pros and cons and shared with Patrick.

=== Rework live-build deb822 PR ===
Date: 2026-01-08

Made several changes to the PR, including adding Trixie to the list of distros continuing to use the one-line apt list format, adding options for overriding the default deb822 sources choice, and making several other adjustments at Roland's request. Discussion on some points of the PR is still pending.

=== Initial review of pre.sh and get_colors.bsh scripts ===
Date: 2026-01-08

Read through both scripts in helper-scripts, made some minor improvements. Both looked generally good.

== 2026-01-07 ==
=== Fix torified repos not being enabled properly by virtualbox-installer-cli ===
Date: 2026-01-07

Discovered a bug in dist-installer-cli which prevented virtualbox-installer-cli from setting up torified Oracle repos even when the system's repos were torified. Fixed it, and also fixed a mistyped variable name.

=== Investigate, revert default speaker volume changes ===
Date: 2026-01-07

Discovered that it is difficult (almost prohibitively so) to set the default speaker volume under Kicksecure 18 KVM due to spice-vdagent. Also discovered that too-high volume concerns are likely less of a concern on physical hardware than previously thought. Reverted the not-quite-functional changes made yesterday and mentioned on the forums why.

=== Add MOK enrollment checks to sysmaint-panel and systemcheck ===
Date: 2026-01-07

Made it so that systemcheck can now inform the user if the MOK exists but is not yet enrolled, and made sysmaint-panel hide the MOK enrollment button if the MOK is already enrolled.

=== Actually revert failsafe boot mode commits ===
Date: 2026-01-07

I thought I had already done this, but it turns out I failed to force-push to finish the revert. Did this, also did usual time tracking and answering people on the forums.

== 2026-01-06 ==
=== Reduce default speaker volume on non-Qubes platforms, discuss sandboxing mechanisms ===
Date: 2026-01-06

A user on the forums pointed out that the speaker volume defaulting to 100% could be unsafe and blow a user's speakers. Added a systemd user unit that sets the volume to 50% on login.

While implementing this, also talked with Patrick about how we want application sandboxing to work in the long run, so that we could figure out how to work with Tor Browser file manager issues in the short term.

=== Review Tor SOCKS auth extension code in dist-installer-cli ===
Date: 2026-01-06

Looked at the code Patrick wrote for moving to the new method of SOCKS authentication. Looked good to me, suggested a variable name change.

=== Add easy MOK enrollment button to sysmaint-panel ===
Date: 2026-01-06

On systems with Secure Boot enabled, sysmaint-panel will now provide a button for setting up and enrolling a MOK easily. This should make Kicksecure use on Secure Boot systems much easier.

== 2026-01-05 ==
=== Finish outstanding systemcheck silenced error investigation ===
Date: 2026-01-05

Looked at the last few error messages we were silencing but not documenting why, added documentation to some of the locations, and stopped silencing one error that no longer seems to occur.

=== Investigate next steps for adding GRUB support to Debian's Raspberry Pi support packages ===
Date: 2026-01-05

Looked at flash-kernel, read through some of the code of raspi-firmware's scripts, and looked at some other PRs that were filed by another Debian contributor (Kurva). Sent a follow-up email to an older thread on this topic, with plans for what to do next.

=== Make VirtualBox installation on Kicksecuree 18 more streamlined ===
Date: 2026-01-05

Added code to usability-misc that symlinks the MOK keys to the location VirtualBox expects to find them. Also added some extra Secure Boot related checks to systemcheck, and documented the installation of mokutil on Kicksecure's wiki.

=== Add timeout to wait process mechanism in wlr_resize_watcher ===
Date: 2026-01-05

Added an option to wlr_resize_watcher for setting the maximum number of seconds it will wait for dependency processes to start.

== 2026-01-04 ==
=== Test Kicksecure 18 compatibility with loopback boot ===
Date: 2026-01-04

Found that loopback boot worked properly on BIOS systems, but was broken on UEFI systems. Found a workaround and filed a bug report with Super GRUB2 Disk.

=== Investigate VirtualBox usability on Kicksecure 18 with Secure Boot ===
Date: 2026-01-04

A user reported that VirtualBox did not work for them with Secure Boot enabled. Reproduced the bug, found a workaround, and discussed next steps with Patrick.

=== Rebase GRUB co-installability patch on current Debian GRUB git master ===
Date: 2026-01-04

Recreated the patch using the original version as a guide. Ensured the new version worked properly. Also fixed an FTBFS bug so I could test, and submitted the fix for that as well.

=== Test, request review on grml-debootstrap Raspbery Pi support ===
Date: 2026-01-04

Ensured both SD card and USB installations booted and worked as intended. Hopefully this will be ready for merge now.

== 2026-01-03 ==
=== Restart work on grml-debootstrap Raspberry Pi support ===
Date: 2026-01-03

Merged grml-debootstrap's master branch into my arraybolt3/rpi branch, removed an unnecessary comment, and began testing. Will continue testing tomorrow.

=== Fix policy-rc.d handling in live-build PR ===
Date: 2026-01-03

A long time ago, I made a PR for live-build that fixed policy-rc.d handling, but it was flawed as a result of supporting both dpkg-divert and update-alternatives methods of working with policy-rc.d. Fixed the flaws by removing dpkg-divert support, built a test image to make sure it worked, and submitted the change upstream.

=== Research using GPT by default in Calamares on BIOS systems ===
Date: 2026-01-03

Using GPT on BIOS systems is not yet a good idea because we don't have hybrid BIOS/EFI bootloader support in Calamares yet. (The PR for it is merged, but a version of Calamares with that PR merged in is not present on Debian.) Documented in the task itself why we shouldn't switch Calamares to using GPT on BIOS yet.

=== Finish wlr-resize-watcher race condition fix, fix default_dispvm bug in Qubes-Whonix ===
Date: 2026-01-03

Tested the fix for preventing a wlr-resize-watcher and pcmanfm-qt race condition under Kicksecure on KVM. Seems to work correctly both on existing systems and new builds (although the default resolution of 1920x1080 is no longer applied on Kicksecure because the default native resolution under virt-manager is being used).

Also got pinged by Marek on the Qubes forums about an issue where the default_dispvm for new Whonix-Workstation AppVMs was not being set properly when making AppVMs from a cloned Whonix-Workstation 18 TemplateVM that had a name other than "whonix-workstation-18". Found and fixed the bug in qubes-core-admin-addon-whonix.

== 2026-01-02 ==
=== Work on fixing PCManFM-Qt desktop coverage issue on first boot on KVM ===
Date: 2026-01-02

PCManFM-Qt provides the desktop under LXQt. wlr_resize_watcher resizing the display to 1920x1080 sometimes causes problems on the first (and later?) boots of Kicksecure under KVM, PCManFM-Qt's desktop ends up stuck in a corner of the screen rather than covering the whole screen. Attempted to create a fix for this, it's working now but needs better-tested before pushing.

=== Research D-Bus activation and PCManFM-Qt ===
Date: 2026-01-02

The "Open in folder" button in the downloads menu of Tor Browser was not working as expected. This turned out to be because PCManFM-Qt needs to be running (optionally in daemon mode) to make this feature work right. Investigated how to start it on-demand using D-Bus activation; this almost worked, but unfortunately not quite. Reported results upstream in the hopes that this can be officially supported later.

=== Revert IPv6 rule omission in whonix-firewall when IPv6 is disabled in the kernel ===
Date: 2025-01-02

Reverted a commit to whonix-firewall, the omission of IPv6 rules was not necessary, and Patrick noted that it could cause problems in the future.

=== Finish browser-choice unprivileged installation support ===
Date: 2026-01-02

Finished testing and fixing up browser-choice, pushed changes to make it able to install browsers without admin privileges on the live ISO.

== 2026-01-01 ==
=== Test, fix bugs in browser-choice plugins and unprivileged installation support ===
Date: 2026-01-01

Didn't quite get the changes to browser-choice into a releaseable state, but found both new and existing bugs and fixed them. A couple minor issues still need sorted out, but once those are done, this should be ready to push.

== 2025-12-31 ==
=== Start adding unprivileged installation command support to browser-choice ===
Date: 2025-12-31

To allow browser-choice to work on live ISO user sessions, I did a lot of work making it so that each browser-choice plugin can specify both privileged and unprivileged installation commands, and detect which set should be used at runtime. Still in testing, not pushed yet.

=== First attempt to specify intended behavior of Qubes supporting VM restrictions feature ===
Date: 2025-12-31

Attempted to map out how a feature allowing one to restrict the allowable templates, NetVMs, and DispVMs of a qube should work. Discussed with Marek in the Qubes bug report.

=== Finish kernel-level IPv6 disabling support for Whonix ===
Date: 2025-12-31

It is now possible to disable IPv6 with sysctl and have Whonix still function properly. One must also disable it at the network level to prevent networking.service from failing during bootup, but once that is done, things work.

== 2025-12-30 ==
=== Start work on allowing kernel-level disabling of IPv6 on Whonix ===
Date: 2025-12-30

Started changing various things (mainly moving Tor configuration around) to make it possible to disable IPv6 with a sysctl config file.

=== Improve Whonix-Installer UX, add more error handling improvements ===
Date: 2025-12-30

Made several changes to Whonix-Installer's UX as suggested by Patrick. Also added better error reporting to several of the exception handlers. Unfortunately, I encountered issues with Windows Smart App Control blocking the installation of Whonix-Starter, and also found that the current code used by Whonix-Installer resulted in an access violation exception if the details window is opened at the wrong time.

=== Fix Whonix UTM bug with double .raw filename extension ===
Date: 2025-12-30

A user reported on the Whonix forums that the UTM images weren't working due to a double .raw filename. Found the root cause and fixed it.

=== Diagnose Tor Browser failing to start in Whonix-Workstation StandaloneVM ===
Date: 2025-12-30

User was most likely booted into sysmaint mode on accident. Correct solution is to boot into user mode. Suggested to Marek that we should make it so that Whonix-Workstation can reconfigure itself when it detects it's a StandaloneVM, and add code to qubes-core-admin-addon-{whonix,kicksecure} to do part of the reconfiguration upon VM creation.

== 2025-12-29 ==
=== Revisit raja-grewal security-misc PRs ===
Date: 2025-12-29

Merged in the log_martians PR, made comments on the remaining PRs. The one enabling AMD SME may not be mergeable, but the other one should be with some fixes.

=== Follow-up on grml-debootstrap deb822 PR ===
Date: 2025-12-29

Checked with zeha about what still needs done for the deb822 PR to be mergeable. It looks like there isn't much left to do. Another Debian contributor has forked my deb822 PR to add additional enhancements, so that may be the PR that gets merged (which is fine, the additional changes are unlikely to cause problems for us).

=== Review disable_kvm_virt_at_load improvements ===
Date: 2025-12-29

Patrick recently adjusted dist-installer-cli so that it would not try to remove the KVM kernel modules unless they were actually loaded. Reviewed the code, made one minor improvement to one function.

=== Fix systemcheck failure due to privleapd printing an INFO message in check-config mode ===
Date: 2025-12-29

systemcheck was erroring out because privleapd was outputting an INFO mesasge about /usr/local/etc/privleap/conf.d not existing at startup, even when started in check-config mode. Modified privleapd to not print the message in check-config mode.

=== Implement requested changes to set-keyboard-layout.sh LUKS warning ===
Date: 2025-12-29

Patrick reviewed the LUKS warning code in our keyboard layout change script, and requested some changes. Implemented all changes.

=== Request Fil-C to split into packageable packages ===
Date: 2025-12-29

Filed a feature request with Fil-C, requesting that the various sub-projects be separated into pieces that can be packaged for Debian.

== 2025-12-28 ==
=== Document colors used in the login security table ===
Date: 2025-12-28

Documented on the Wiki each color that is used, what values are presented in those colors, what the colors mean, and why they are used.

=== Investigate disabling IPv6 on Whonix 18 ===
Date: 2025-12-28

Discovered that it was possible to disable IPv6 on Whonix 18 even on non-Qubes platforms by changing network configuration. Also noted that this did not completely disable IPv6. Asked Patrick if it was worth doing the work needed to allow completely disabling IPv6 at the kernel level.

=== Polish grml-debootstrap deb822 support PR ===
Date: 2025-12-28

zeha reviewed the deb822 support PR and requested some changes. Implemented those changes.

=== Review AI PRs against tb-updater ===
Date: 2025-12-28

Added the useful improvements suggested by ChatGPT Codex, provided rationale for rejecting the other changes.

=== Add confirmation prompt when changing keyboard layout when full disk encryption is in use ===
Date: 2025-12-28

Changing the keyboard layout comes with the risk of causing a system lockout when used with full disk encryption. Added code to the keyboard layout change routine to detect when this risk exists and warn the user if needed.

=== Fix genmkfile debinstfile inconsistent behavior on different systems ===
Date: 2025-12-28

We were using <code>find</code> without sorting its output, so the order in which it wrote files to Debian install files was semi-random. Diagnosed the issue, and added a <code>sort</code> command to resolve the issue.

== 2025-12-27 ==
=== Investigate user bug reports for Kicksecure and Whonix 18 ===
Date: 2025-12-27

Looked at several odd behaviors that were occurring, making fixes for some issues, suggesting next steps for others, and filing a bug report for one (a Tor Browser issue where wrong user interaction can cause an apparent freeze).

=== Disable sudo DNS lookups on desktop platforms ===
Date: 2025-12-27

Discovered that the reason sudo was attempting DNS lookups was because the <code>fqdn</code> option was enabled. Disabled it in security-misc-desktop. Did not disable it in security-misc-server since it could break server machines in unexpected ways.

=== Use package-installed-check in sysmaint-panel ===
Date: 2025-12-27

New code in sysmaint-panel was sourcing the package_installed_check.sh library and then calling the function in it, rather than using the wrapper script. Simplified the code by using the wrapper script.

=== Fix user-reported sdwdate seccomp filter issue ===
Date: 2025-12-27

Someone on the forums reported that sdwdate was crashing for them because of three missing syscalls in the seccomp filter. Was not able to trivially reproduce, but the syscalls looked benign, so they have now been added.

== 2025-12-26 ==
=== Make privleap able to parse config files under /usr/local/etc ===
Date: 2025-12-26

Changed the configuration parser to handle a list of configuration files, added /usr/local/etc/privleap/conf.d to the list, and adjusted the test suite to handle the changed error messages and test the use of configuration files in the new directory.

=== strict_config_parser AI review, improve test suite ===
Date: 2025-12-26

ChatGPT didn't find bugs in the library itself, but did find areas where the test suite could be improved. Implemented the suggested test improvements and ensured the new tests passed.

=== Discuss Whonix on QEMU on Windows, investigate performance ===
Date: 2025-12-26

Recently I discovered that QEMU would be used on Windows accelerated with Hyper-V. Discussed how we could turn this into a VirtualBox alternative with Patrick, and did some performance testing (making sure video playback worked reasonably well).

== 2025-12-25 ==
=== Finish improving anonymity of dynamic resolution resize helper ===
Date: 2025-12-25

Finished getting the code working and tested and pushed it. Created a follow-up task for doing an AI review of strict_config_parser.

== 2025-12-24 ==
=== Improve anonymity of dynamic resolution resize helper (work-in-progress) ===
Date: 2025-12-24

Created a configuration parsing library for use in future software, edited sdwdate-gui to use it. Added configuration options to wlr_resize_watcher, using this new library for parsing config files. Discovered that VirtualBox made it difficult to set an non-fingerprintable display resolution, and started research on how to resolve this.

=== Finish deb822 wiki updates ===
Date: 2025-12-24

Both Kicksecure and Whonix wikis should now be fully updated for the deb822 sources format.

== 2025-12-23 ==
=== Begin updating the wiki for deb822-style sources format ===
Date: 2025-12-23

A report from a user on the forums indicated that our documentation still assumed the old, one-line-style sources format was used everywhere. Started updating both Kicksecure and Whonix wikis to use the deb822-style format wherever possible.

== 2025-12-22 ==
=== File systemd bug report ===
Date: 2025-12-22

As discussed.

=== Disable automount in pcmanfm-qt, report the new mechanism on the forums ===
Date: 2025-12-22

pcmanfm-qt was automounting removable media due to an oversight during configuration. This is now fixed, and the mechanism we use is now documented on the forums.

=== Push virtiofs support in vm-config-dist, document use on Whonix wiki ===
Date: 2025-12-22

The documentation now covers how to use both virtio-9p and virtiofs, and both are supported by vm-config-dist.

== 2025-12-21 ==
=== Research switching to virtiofs for KVM shared folders ===
Date: 2025-12-21

virtiofs appears to have a low attack surface, and behaves in a much more intuitive way than virtio-9p, but it also allows a guest to write SUID-root executables to the host (which could be a danger in a social engineering attack where the user is asked to run something from the shared folder on the host). Unsure if this danger outweighs the advantages of virtiofs (ability to use tighter folder permissions, among other things).

=== Whonix wiki updates 5 (finish first pass of wiki updates) ===
Date: 2025-12-21

The first pass of the wiki updates for Kicksecure/Whonix 18 are now done. MultiWiki pages have been updated.

== 2025-12-20 ==
=== Whonix wiki updates 4 ===
Date: 2025-12-19

Still working on updating the wiki. Mainly got the KVM page polished and updated the Project Networking page.

== 2025-12-19 ==
=== Whonix wiki updates 3 ===
Date: 2025-12-19

Continued work on updating the Whonix wiki for Whonix 18.

== 2025-12-18 ==
=== Whonix wiki updates 2 ===
Date: 2025-12-18

Continued work on updating the Whonix wiki for Whonix 18.

=== Report QTerminal susceptibility to Trojan Source attacks ===
Date: 2025-12-18

Discovered that QTerminal's "confirm multiline paste" dialog would render malicious Unicode in a way that could fool a user into believing the code would do one thing when it would actually do something else. Reported the bug upstream (first privately, then publicly once given permission).

=== Add lxqt-localization packages to appropriate images ===
Date: 2025-12-18

This should make non-English installations work better in general (more translated strings in the UI).

=== Add gvfs-backends to GUI images ===
Date: 2025-12-18

This enables pcmanfm-qt's "Admin mode" to work in sysmaint sessions (or in user sessions if running in unrestricted admin mode).

== 2025-12-17 ==
=== Fix QTerminal bookmark save location ===
Date: 2025-12-17

Due to an oversight, QTerminal was being configured by default to always save its bookmarks in <code>/home/user</code>, regardless of which user account QTerminal was running under. Created a fix for this.

=== Finish initial Kicksecure wiki update pass, start updating Whonix wiki ===
Date: 2025-12-17

Most of Kicksecure's wiki should now be updated for Kicksecure 18, though there may still be pages here and there that need reviewed and updated. Working through Whonix wiki now.

=== Research, attempt to fix apparent msgcollector hang on shutdown ===
Date: 2025-12-17

Made msgdispatcher continuously try to notify systemd that it has started until it succeeds at doing so.

=== Fix QTerminal multi-line paste warning ===
Date: 2025-12-17

Set the system-wide "ConfirmMultilinePaste" setting for QTerminal to "true", and added code to legacy-dist to enable it for all existing users.

== 2025-12-16 ==
=== Wiki updates continued further ===
Date: 2025-12-16

Finished working through all pages on the Kicksecure wiki that mention LXQt. Continuing to work through pages that refer to Xfce, and fixing other pages that need updated as I run across them.

== 2025-12-15 ==
=== Wiki updates continued ===
Date: 2025-12-15

Still working through wiki pages, fixing outdated documentation, link, package names, etc. Almost done working through all pages on the Kicksecure wiki that mention LXQt.

== 2025-12-14 ==
=== More Kicksecure/Whonix 18 wiki updates ===
Date: 2025-12-14

Updated a lot more of the wiki, especially overhauling ISO installation and input method instructions.

=== Merge more security-misc PRs from raja-grewal ===
Date: 2025-12-14

Reviewed and merged in two more PRs.

== 2025-12-13 ==
=== Continue Kicksecure/Whonix 18 wiki updates ===
Date: 2025-12-13

Fixed more outdated documentation, did more work to make sure that the global Xfce/LXQt replacement didn't leave the wiki in a damaged state. More work needed to get everything complete

=== Revisit security-misc PRs from raja-grewal ===
Date: 2025-12-13

Merged a couple more PRs, commented on further steps needed for another PR.

== 2025-12-12 ==
=== Start updating the wikis to be up-to-date for Kicksecure and Whonix 18 ===
Date: 2025-12-12

Started polishing our documentation (especially in the area of metapackage names) to be up-to-date for Kicksecure/Whonix 18. Discovered some issues in the process with some automated quick conversion.

== 2025-12-11 ==
=== Disable unrestricted admin mode in Qubes OS templates, pop up a warning if one attempts to use the mode ===
Date: 2025-12-11

If a user attempts to boot a TemplateVM in "UNRESTRICTED Mode", it will now boot into a standard user mode and display a warning popup via systemcheck. (Previously, it removed user-sysmaint-split from the template, which persisted into future reboots.)

=== Investigate installation crash on VirtualBox on Windows ===
Date: 2025-12-11

Could not reproduce issue, shared with Patrick what it looked like was possibly happening.

=== Add the ability to turn off panic-on-oops to security-misc and sysmaint-panel ===
Date: 2025-12-11

Made it possible to turn off panic-on-oops.service using systemctl (both immediately and for future boots). Also added a button to sysmaint-panel for quickly toggling this. Restructured how the sysmaint-panel UI is built in order to make adding more buttons easier in the future, and also fixed the user deletion button.

== 2025-12-10 ==
=== Document most ignored warnings in systemcheck ===
Date: 2025-12-10

Wrote down why each warning silenced in systemcheck's journal check is silenced. Made notes and follow-up tickets for ones that looked harder to figure out.

=== Submit policyrcd-script-zg2 changes for more flexable use upstream ===
Date: 2025-12-10

Added config directory support to policyrcd-script-zg2, tested it, and submitted it upstream. Once upstream accepts it, we can upload it into Kicksecure's archive and use it.

=== Create PR to enable shutdown timeout debugging unit for Qubes-Whonix tests ===
Date: 2025-12-10

Created a PR that enables <code>check-user-slice-on-shutdown.service</code> on Whonix systems during the testing process.

== 2025-12-09 ==
=== Update bridges in anon-connection-wizard ===
Date: 2025-12-09

The list of bridges in anon-connection-wizard was out-of-date and wasn't working right. Updated the list with the latest list from the Tor Project's tor-browser repository.

=== Review, improve and fix some problems with keyboard layout set scripts ===
Date: 2025-12-09

Looked at Patrick's recent work on set-keyboard-layout.sh in helper-scripts. Fixed some issues, consolidated some redundant code, and ensured both the Kicksecure ISO and Kicksecure KVM image built properly and had GRUB keyboard layouts available.

=== Make package installation in sysmaint-panel more reliable, file lxqt-archiver bug report ===
Date: 2025-12-09

The package installation process now updates apt lists if a package can't be found initially. It also lowercases the package name, since all packages in the Debian archives use all-lowercase names and this avoids problems if users type names that contain capital letters.

Also filed a bug report against lxqt-archiver, which is unable to open RAR files in Trixie.

== 2025-12-08 ==
=== Further review of tor-control-panel merge ===
Date: 2025-12-08

Unfortunately, it does not look like this is going to be mergeable yet. Notified troubadour of this.

=== Debug block-unsafe-logins failure on Qubes OS ===
Date: 2025-12-08

The user who encountered this was trying to use an in-VM kernel, which doesn't work with boot modes under Qubes OS yet.

== 2025-12-07 ==
=== Start review of merged tor-control-panel / anon-connection-wizard ===
Date: 2025-12-07

Started reading through the new code from troubador. Will make modifications as needed to get it working right.

=== Investigate application installation difficulty, mis-reporting of FDE ===
Date: 2025-12-07

Researched a couple of bug reports from the forums, fixed one of the bugs, and commented on a possible solution for another issue (which wasn't really a bug but was more of a usability issue).

=== Finish integrating Hyper-V disable scripts into Whonix-Installer ===
Date: 2025-12-07

Fixed UI issues, script issues, and code issues. The application now builds without problems on Debian Trixie, and the installer can disable (or partially re-enable) Hyper-V as desired.

=== Look at issues with dpkg-new files in /etc/grub.d ===
Date: 2025-12-07

Patrick noticed that it was possible to end up with a <code>.dpkg-new</code> file stuck in /etc/grub.d. This looks like it might be problematic. More discussion needed to determine if this is a problem.

=== Add stuck process checker to usability-misc ===
Date: 2025-12-07

There is now a systemd service in usability-misc that, when enabled, will report if processes under <code>user.slice</code> are stuck during shutdown, and if so, which ones.

=== Review, update wiki pages on immutability ===
Date: 2025-12-07

Used some already-referenced material and 3mdeb's research into immutable distributions to make our wiki pages more correct. (In particular, grub-live is not an immutable boot mode, but a stateless boot mode.)

=== Discuss how to fix Tor Browser automatic update reliability ===
Date: 2025-12-07

Commented on a Qubes OS bug report about Tor Browser automatic updates, trying to figure out how to allow automatic updates to work in a secure fashion that requires less human intervention.

=== More polish on unicode-show and related tools ===
Date: 2025-12-07

Fixed the return value of unicode-show when a Unicode decode error is found (I mistakenly thought ChatGPT changed this incorrectly, when it was actually correct), and made unicode-show, sanitize-string, strip-markup, and the stdisplay family of tools not directly executable via /usr/lib/python3/dist-packages, to avoid inconsistent behavior. (Users should call these using their wrapper scripts in /usr/bin.)

== 2025-12-06 ==
=== Finish regression tests for unicode-show ===
Date: 2025-12-06

ChatGPT Codex created a test suite for unicode-show. It looked useful, but did not work the way most of our other tests work. Wrote a full test suite using ChatGPT's example code as a guide, and ensured it passed as expected.

== 2025-12-05 ==
=== Work on fixing carriage return concerns in helper-scripts sanitize-string and stdisplay ===
Date: 2025-12-05

We've decided to treat carriage returns as potentially malicious in stdisplay and related utilities. Did a lot of the work needed to make these changes work and make working regression tests for them.

=== Research creation of a debug helper unit for shutdown ===
Date: 2025-12-05

We'd like to create a systemd unit that triggers on shutdown and outputs information about still-running processes and units once per second until all processes but itself have terminated. Did some research to figure out if this was feasible, and how to do it without stalling shutdown for an unreasonably long time.

== 2025-12-04 ==
=== Fix sdwdate-gui-qubes-proxy stuck in "activating" state ===
Date: 2025-12-04

When sys-whonix was powered off but anon-whonix was on, anon-whonix had an sdwdate-gui-qubes@.service unit stuck "activating" until sys-whonix was powered back on. This caused issues in systemcheck. Found the root cause of this and fixed it.

=== Continued Codex PR review ===
Date: 2025-12-04

Merged more changes to helper-scripts and security-misc. Discussed some aspects of the changes with Patrick.

== 2025-12-03 ==
=== More Codex PR review ===
Date: 2025-12-03

Still working through PRs mostly to helper-scripts. Was able to merge in several changes.

=== Simplify whonix-firewall restarter with systemd settings ===
Date: 2025-12-03

The enhancements to the restarter script from ChatGPT ended up being superfluous because of the systemd unit configuration on firewall-restarter. Reverted the changes, and added <code>PrivateTmp=true</code> to the unit to clean up fifos.

== 2025-12-02 ==
=== Continued Codex PR review ===
Date: 2025-12-02

Reviewed and merged in the useful parts of additional Codex PRs. This notably included additional test enhancement for stdisplay, and a resource consumption optimization for sttee.

=== Add additional defense-in-depth measures to sanitize_string ===
Date: 2025-12-02

ChatGPT warned that there was the possibility of unsanitized ANSI escape codes being printed by sanitize_string if a malicious string contained ESC characters encoded as HTML entities. This did not appear to be a real concern (such entities were simply stripped by markup stripping rather than being translated), but just in case this behavior changed in the future, or a workaround for embedding ESC characters into HTML exists, there is now an extra stdisplay call to sanitize ANSI codes after markup stripping.

=== Fix broken screen unlock caused by block-unsafe-logins ===
Date: 2025-12-02

swaylock runs the PAM stack as a non-root user, which broke block-unsafe-logins and made it think every user account was passwordless. Fixed this by adding a privleap call to block-unsafe-logins for getting password status information.

== 2025-12-01 ==
=== Start reviewing more Codex PRs ===
Date: 2025-12-01

Patrick ran more of our software through ChatGPT Codex, which made several more PRs. Started reviewing those, merging in code where appropriate.

=== Silence SRSO warning in virtual machines and containers, advise what to do about watchdog timeouts ===
Date: 2025-12-01

Patrick noted that there were strange watchdog timeouts happening after massive time jumps, and very long CPU soft lockups in a swap-related kernel thread. Mentioned that this looked to be either hardware- or configuration-related. Also silenced SRSO warnings in virtual machines since they do not appear to be entirely fixable there.

=== Make set-system-keymap also set the GRUB keymap ===
Date: 2025-12-01

Setting the system keymap with <code>set-system-keymap</code> will also configure GRUB to offer the chosen keyboard layout in the boot menu now.

=== Fix tor-ctrl end-of-options, add to Kicksecure by default ===
Date: 2025-12-01

tor-ctrl's argument parser was subtly broken such that the end-of-options indicator was being incorrectly parsed. Fixed this, and also added tor-ctrl to dist-general-cli so it would be installed on Kicksecure as well.

=== Final test and code approval on DispVM PR for qubes-core-admin-addon-whonix ===
Date: 2025-12-01

The code appears to be working correctly. Approved the PR and posted the test results.

=== Work new sanitize-string and strip-markup commands into codebase, expand tests ===
Date: 2025-12-01

Added some more tests for sanitize-string to test the string truncation feature. Modified the entire codebase to use the new API for sanitize-string and strip-markup where appropriate.

=== File bug report for Qubes Salt issues with anon-whonix.sls ===
Date: 2025-12-01

Filed a bug on the Qubes OS bug tracker with all info about <code>qubesctl</code> failing to create default Whonix virtual machines even with necessary templates preinstalled.

== 2025-11-30 ==
=== Add tests for strip_markup and sanitize_string, add to helper-scripts test suite ===
Date: 2025-11-30

Wrote working tests for strip_markup and sanitize_string. Found and fixed a bug in sanitize_string in the process. Still need to work new APIs for strip_markup and sanitize_string into the codebase.

=== Add notification to sysmaint qrexec action blocker ===
Date: 2025-11-30

user-sysmaint-split's qrexec action blocker now creates a desktop notification when an action is blocked, helping to reduce user confusion.

=== More debugging missing boot menu options after upgrading standalone Whonix-Workstation from Whonix 17 to 18 ===
Date: 2025-11-30

Issue currently appears to be an upgrade bug when using a Whonix standalone that originally did not have user-sysmaint-split installed. More research needed.

=== Finish testing of DispVM PR for qubes-core-admin-addon-whonix from Ben ===
Date: 2025-11-30

Tested a modified version of the PR, seems to work acceptably well. Published my modifications, Ben merged them.

=== Attempt to debug Whonix Salt formula failure on Qubes R4.3 ===
Date: 2025-11-30

Looked at the code for the salt formulas, tested on a couple of different machines, and investigated some of the commands involved in the process. Was not able to determine why the Salt formula is failing. Will file a bug report with Qubes later.

== 2025-11-29 ==
=== Review security-misc PRs from raja-grewal ===
Date: 2025-11-29

Merged in several more PRs, commented on those that could not be merged yet.

=== Re-review, attempt to test ben-grande's DispVM/NetVM config pr to qubes-core-admin-addon-whonix ===
Date: 2025-11-29

Found some possible bugs in the PR and some pre-existing bugs that would interfere with the PR's goals but weren't in the PR itself. Made some fixes, reported the issues as another review. Attempted to test a modified version of the PR, but got stuck because Salt did not allow me to regenerate the Whonix VMs the way I expected.

=== Rebuild Whonix-LXQt 18 image for tb-updater testing ===
Date: 2025-11-29

Built the Whonix 18 images for VirtualBox from scratch to ensure that Tor Browser was installed properly in the workstation during the build process.

=== Further enhance the workaround for the LXQt black desktop background bug ===
Date: 2025-11-29

A completely empty <code>~/.config/pcmanfm-qt/lxqt</code> directory was able to trigger the same bug. Added code for detecting and removing this directory if empty.

== 2025-11-28 ==
=== Use the hardcoded Tor Browser version in tb-updater as a verification mechanism rather than a locking mechanism ===
Date: 2025-11-28

Changed the tb-updater code a bit so that newer versions of tb-updater could be downloaded during image builds even if the hardcoded version was out of date.

=== Attempt to debug boot menu entries not appearing in Whonix-Workstation 18 standalone VM ===
Date: 2025-11-28

Was unable to reproduce the problem by making a standalone from an existing template. Found the corresponding bug report on the Qubes OS issue tracker and found out this was an upgrade issue, suggested that perhaps user-sysmaint-split needed to be installed.

== 2025-11-27 ==
=== Start reviewing several security-misc PRs from raja-grewal ===
Date: 2025-11-27

raja-grewal made several PRs against security-misc that were in need of review. Started going through them, commenting on them and merging some of them in.

=== Investigate suspend failure with Kicksecure 18 ===
Date: 2025-11-27

Could not reproduce the exact issue, but it may be caused by <code>panic_on_warn</code>, which caused problems on my current testing machine.

=== Find root cause of network settings issue on Qubes HVMs, document fix and how to avoid ===
Date: 2025-11-27

The user who reported the network settings issue with Qubes HVMs was disabling "All users may connect to this network". I was able to reproduce the issue when doing this. Documented that this should not be done, what the results of doing it are, and how to undo it.

== 2025-11-26 ==
=== Investigate network settings not carrying over from sysmaint to user mode ===
Date: 2025-11-26

Was not able to reproduce the issue; network configuration done in user mode was visible in sysmaint mode and vice-versa.

=== Make leapctl and leaprun poll for the appropriate privleapd socket before giving up ===
Date: 2025-11-26

Patrick discovered he could cause leaprun to error out unexpectedly by running <code>sudo systemctl restart privleapd.service; leaprun ...</code>. Discussed possible solutions, ended up implementing simple polling for the appropriate sockets.

=== Update kloak documentation on Whonix wiki ===
Date: 2026-11-26

Read through existing kloak documentation, updated the docs in several areas.

=== Finish kloak Tor Browser scrolling fix ===
Date: 2025-11-26

The reworked scroll event handling appears to be working well with multiple types of scrolling devices (standard mice, an MX Master 3S, vertical and horizontal two-finger scrolling on a touchpad, and KVM's mouse input). Didn't find any memory safety issues, did find and fix a problem with unexpected libinput events thwarting the anonymization of scroll events. Merged to arraybolt3/trixie branch and posted an update on the Whonix forums.

== 2025-11-25 ==
=== Rework scroll handling in kloak ===
Date: 2025-11-25

The original way scroll events were handled turned out to not work with Tor Browser or Firefox, and was less anonymous than it could be. Reworked the scrolling mechanism to work with Tor Browser, and made scroll handling more anonymous as a side effect.

== 2025-11-24 ==
=== Continue Whonix-Installer Hyper-V disable work ===
Date: 2025-11-24

The logic seems to be mostly right at this point. There are some strange UI issues that need sorted out still.

=== Review, integrate whonix-firewall changes suggested by ChatGPT Codex ===
Date: 2025-11-24

Patrick had Codex look at whonix-firewall. It suggested some fixes and enhancements, many of which looked valuable though most of them needed changes to be suitable for inclusion. Reviewed, studied, and integrated the suggestions as appropriate.

== 2025-11-23 ==
=== Start adding Hyper-V disable functionality to Whonix-Installer ===
Date: 2025-11-23

A lot of the refactoring needed to disable Hyper-V in Whonix-Installer is now done. I'm also adding enhanced error handling to some of the code since it does not look particularly safe in its current state.

=== Debug, fix pkexec SUID bit not being restored when user-sysmaint-split is uninstalled ===
Date: 2025-11-23

Due to an unfortunate interaction between permission-hardener's first-run timing and pkexec's postinst script, the permissions on /usr/bin/pkexec were 755 (no SUID bit) when permission-hardener first locked it, resulting in the incorrect permissions being restored later. After discussion with Patrick, I fixed this by making security-misc's postinst install a "good" initial state during the package's installation, and also added a str_replace call for fixing the permission database on upgrade.

=== Make sdwdate-gui-qubes@.service a notify unit ===
Date: 2025-11-23

Using a oneshot unit here meant that the unit always showed as starting when it was likely not going to ever terminate. Switched to <code>Type-notify</code> and added a systemd-notify call to remedy this.

== 2025-11-22 ==
=== Silence more journal warnings in systemcheck for Whonix on VBox and Qubes ===
Date: 2025-11-22

Looked at both Whonix-Gateway and Whonix-Workstation on VirtualBox and Qubes, found ignorable journal warnings, and added them to the ignore list.

=== Debug, fix power management config override in lxqt-powermanagement ===
Date: 2025-11-22

lxqt-powermanagement was overriding system-wide configuration with user-specific configuration because the system-wide configuration didn't declare that lxqt-powermanagement was already configured. Determined what option needed to be set to fix this, and set it. Suggested a way to fix the configuration for existing Kicksecure/Whonix 18 users.

=== Silence spurious sdwdate-gui-qubes-proxy systemd unit failures in Kicksecure-based sys-net ===
Date: 2025-11-22

sys-net starts before sys-whonix, thus sdwdate-gui has no way of reporting its status to sys-whonix upon initial boot. Changed an <code>exit 1</code> to an <code>exit 0</code> to resolve this. Also noticed an issue with Tor status reporting and fixed it while I was working on this.

=== Document boot menu theme disappearing when serial-console-enable is installed ===
Date: 2025-11-22

Reproduced the boot menu theme issue on my end, documented it on the recovery wiki page (which is where we tell users to install serial-console-enable).

=== Test usbguard accept/reject prompts in in Kicksecure-based sys-usb ===
Date: 2025-11-22

Patrick added "modify" permissions to our usbguard IPC configuration. Tested and verified that the usbguard-notifier prompts now allowed accepting or rejecting USB devices when Kicksecure was used as sys-usb.

=== Finish debugging Kicksecure ISO HVM issues, create bug reports ===
Date: 2025-11-22

Determined why the virtual VGA adapter used for HVMs was not working with labwc (too little VRAM turned out to be the culprit). Also did testing to see what was happening with resolutions like 1366x768 and determine whether it was a Qubes issue, QEMU issue, or labwc issue. Reported bugs to Qubes OS.

=== Review, test replace-ips after refactor ===
Date: 2025-11-22

Patrick substantially refactored the replace-ips script and asked me to review and test. Looked through it, made some minor changes, and tested it. It appeared to work properly on my R4.3 machine.

== 2025-11-21 ==
=== Debug, fix unnecessary Tor restarts during early boot under Qubes OS ===
Date: 2025-11-21

Patrick noticed that Tor was failing to start when sys-whonix first booted under Qubes, and ended up restarting. Cooperatively figured out why, found a solution, and implemented it.

== 2025-11-20 ==
=== More work on optimizing Whonix bootup, audit mkdir calls in an attempt to fix /tmp/user/100* ownership bug ===
Date: 2025-11-20

Found some ways to speed up service startup for some of our sdwdate-related systemd units, and applied those optimizations. Also audited all mkdir calls in Kicksecure and Whonix to see if any of them were at risk of creating /tmp/user/1000 (or similar directories) owned by root. Found one possible mkdir issue, and made a fix for it.

=== Silence red XDG_RUNTIME_DIR error from labwc ===
Date: 2025-11-20

After some more research, decided that the error was benign and could be safely hidden. Added code to do this to desktop-config-dist and systemcheck.

=== Fix sdwdate-gui flashing menu and busy-wait bugs ===
Date: 2025-11-20

The new asyncio version of sdwdate_gui_client was sending too-frequent Tor update messages during a Tor restart, and would consume 100% CPU if the sdwdate-gui.ConnectCheck qrexec call failed. Fixed both bugs.

== 2025-11-19 ==
=== Debug, work around Qubes OS Kicksecure ISO boot failure ===
Date: 2025-11-19

Determined that the reason the Kicksecure ISO was booting to a black screen under Qubes OS was because of wlr_resize_watcher attempting to set the display resolution to 1920x1080 (labwc does not work with this resolution with the emulated graphics in Xen). Modified wlr_resize_watcher to use a resolution of 1024x768 under Xen.

=== Debug, work around BTRFS live mode breakage ===
Date: 2025-11-19

Due to a bug in Debian's version of Dracut, using rd.live.overlay.overlayfs for live mode failed on BTRFS (the filesystem would end up read-only with no overlay). Reported the bug to Debian and switched back to Debian's "rootovl" parameter for the time being. (Unfortunately even this parameter has a cosmetic issue with BTRFS, but functionally it works.)

=== Verify last bug mentioned in privleap AI review task was fixed ===
Date: 2025-11-19

Patrick mentioned another bug related to non-ideal signal handling in leaprun. Looked at and tested the code, verified that the issue no longer occurred in normal use.

=== Add black desktop fix code to desktop-config-dist ===
Date: 2025-11-19

The previous bugfix for the black desktop issue only prevented it from happening, it didn't fix it after it already happened. Added more code to desktop-config-dist that fixes it after it happens.

== 2025-11-18 ==
=== Add systemcheck rule to silence a new benign warning under Qubes OS ===
Date: 2025-11-18

A Qubes OS PR causes the in-domU notification proxy to start earlier, resulting in a small amount of benign log spam that was showing up in systemcheck. Added a new journal ignore rule to silence it.

=== Fix broken passwordless sudo in unrestricted admin mode ===
Date: 2025-11-18

Our new PAM-based user locking mechanism in security-misc was accidentally keeping people from using passwordless sudo if they removed user-sysmaint-split. Added some code to the locking mechanism to bail if user-sysmaint-split is not installed.

=== Review, integrate more suggested fixes to privleap from ChatGPT Codex ===
Date: 2025-11-18

Codex suggested some more useful improvements to privleap, such as dropping supplemental groups (critical, only wasn't a security hole because systemd didn't set any supplementary groups on privleapd) and checking the ownership and mode of the privleap configuration directory and files under it. Applied those enhancements.

=== Improve termination signal handling in leaprun ===
Date: 2025-11-18

Hitting Ctrl+C while leaprun is actively streaming stdio from a privleap action could sometimes cause leaprun to exit improperly rather than sending an action termination message to privleapd. Improved the signal handler mechanism to fix this.

=== Diagnose, fix trigger of black desktop issue with LXQt ===
Date: 2025-11-18

LXQt has a bug of sorts where shutting down the system from the CLI using the reboot or shutdown commands will cause various settings in pcmanfm-qt being set to bad defaults, if the system has never been shut down from within a user session before. This turned out to be the result of a stray lockfile pcmanfm-qt was leaving on the disk. Added code to our desktop launch routine to detect and remove this lockfile during login.

== 2025-11-17 ==
=== Finish fixing qubes-video-companion on Whonix-Workstation 18, work on optimizing Whonix ===
Date: 2025-11-17

qubes-video-companion now works normally on Whonix, awaiting PR merge. There are some warnings from systemcheck that appear likely unrelated to qubes-video-companion that need to be looked at though.

Most of the remaining issues with Whonix tests failing on Qubes OS appear to be the result of Whonix generally being too slow to boot. Did some research into how to optimize our code's performance, especially at boot time.

=== Fix theming of keyboard layout selector in ISO GRUB menu ===
Date: 2025-11-17

Determined why the theming was broken and fixed it. Built a new Kicksecure 18 ISO, the theming looks good now.

== 2025-11-16 ==
=== More work on diagnosing performance issues under Qubes OS, start work on adding privleap rules to qubes-video-companion ===
Date: 2025-11-16

Discovered that VMs other than Whonix were suffering occasional performance issues on Qubes, but that Whonix was the only one tripping timeouts. Got a privleap rule added to qubes-video-companion, but was not yet able to test it due to a problem with qubes-builderv2 and building Debian packages.

=== Integrate security fixes and hardening for privleap from ChatGPT Codex ===
Date: 2025-11-16

Patrick ran privleap through Codex, which found a number of minor security issues in it (all of which were unlikely to cause problems with the way we use privleap in Kicksecure and Whonix). Integrated fixes and regression tests for those fixes into privleap, and ensured all regression tests passed.

== 2025-11-15 ==
=== Benchmark some of Kicksecure's performance issues under Qubes OS ===
Date: 2025-11-15

So far I've been unable to reproduce many of the memory and timeout issues being experienced in Qubes' OpenQA tests. Benchmarking shows some slowed performance, but this appears to be mostly caused by our myriad of systemd units for various hardening tasks, and it's unclear if it's possible or easy to fix this. I couldn't reproduce major issues that would be likely to cause test failures, so I will need to investigate the individual failing tests closer and possibly run the test system locally to try and reproduce.

=== Fix Kicksecure VM image build failure caused by wiped /etc/hostname ===
Date: 2025-11-15

Discovered that when the builder's hostname is <code>localhost</code>, and the built image's hostname is also supposed to be <code>localhost</code>, the build system gets confused and runs <code>shred</code> on the built image's <code>/etc/hostname</code>. Added a check to see if the built image's <code>/etc/hostname</code> matches the intended build hostname, and avoid running <code>shred</code> if so.

== 2025-11-13 ==
=== Add username and UID information to authorization presence/absence notices in leaprun ===
Date: 2025-11-13

Made it so that when leaprun reports that a user is authorized or unauthorized to run a particular action, that they are told the exact username and UID that is authorized or unauthorized. Also did refactoring to make tests easier to work with.

=== Add GRUB keyboard layout configuration support ===
Date: 2025-11-13

Created scripts for building X keyboard layouts into GRUB layout, both at upgrade and image build time. Also integrated the needed features to get these on the ISO boot menu. This should make it easier for users to add custom kernel parameters frmo within GRUB.

== 2025-11-12 ==
=== Improve set-keyboard-layout.sh, research console and GRUB keyboard layout config ===
Date: 2025-11-12

Made it so that other options passed to the keyboard layout change scripts work even when used in combination with <code>--interactive</code>. Implemented initramfs regeneration for set-system-keymap. Changed set-labwc-keymap to persist settings by default and use <code>--no-persist</code> to suppress persistence. Researched whether it would be possible to set the console keymap without root permissions, and how to change the GRUB keymap.

=== swap-file-creator fixes, make livecheck-lsblk.service wait for systemd-tmpfiles ===
Date: 2025-11-12

Fixed some issues after merging in Patrick's changes to swap-file-creator, and made it easier to override the setting that refuses to create a swapfile on non-LUKS systems.

Added an ordering dependency to livecheck-lsblk.service to make it wait for systemd-tmpfiles.

=== Attempt to diagnose USB installation issues, cancel ticket for increasing non-qubes RAM ===
Date: 2025-11-12

A user on the Kicksecure forums reported problems attempting to install Kicksecure to USB drives. This appeared to be a mixture of Rufus behaving poorly and some timeouts being too short.

Now that we know that some of our RAM issues are caused by encrypted swap, we don't have to increase RAM requirements anymore.

== 2025-11-11 ==
=== Adjust swap-file-creator to use unencrypted swap, report bug with encrypted swapfiles ===
Date: 2025-11-11

Modified swap-file-creator to use unencrypted swapfiles, and added a substantial amount of code to it for ensuring that the underlying disk is a LUKS volume. Also verified that the encrypted swapfile lockups do occur with a mainline kernel, and reported the bug to the LKML.

=== Move sdwdate-gui config file migration code to sdwdate-gui postinst ===
Date: 2025-11-11

Moved the migration code from legacy-dist to sdwdate-gui, so that upgrades will go smoothly.

== 2025-11-10 ==
=== Begin debugging VM lockups with encrypted swapfiles ===
Date: 2025-11-10

I discovered that VMs were locking up due to RAM exhaustion before using anywhere near as much swap as was available when using an encrypted swapfile. Confirmed that encrypted swapfiles work on Bookworm, break on Trixie, and than unencrypted swapfiles (whether on or not on a LUKS volume) work as expected. Need to test swapfile behavior under Qubes OS and possibly report a kernel bug for this issue,

=== Fix passwordless login for sensitive accounts ===
Date: 2025-11-10

After some discussion, Patrick and I decided we should only prevent privilege escalation tools from authenticating sensitive passwordless accounts, and that login utilities like greetd, login, and sshd should be allowed to authenticate normally. I implemented this, it seems to work in testing.

=== Investigate Trixie startup/shutdown speed ===
Date: 2025-11-10

Both me and Patrick believed we noticed substantially slowed down shutdown after some recent code changes. I was unable to reproduce the issue however, when measuring time from a shutdown button click until VirtualBox reported "Powered off" using SimpleScreenRecorder and Friction.

=== Merge invalid data handling fixes for sdwdate-gui from ChatGPT Codex ===
Date: 2025-11-10

ChatGPT caught a couple of spots where invalid data could be handled in a less-than-ideal way and suggested good fixes for them. Merged in a modified version of those fixes.

== 2025-11-09 ==
=== Attempt to hide red XDG_RUNTIME_DIR error during shutdown ===
Date: 2025-11-09

Very rarely, something (looks like labwc) complains about XDG_RUNTIME_DIR not being set when it launches. Attempted to resolve this, however my first try failed. Most likely the labwc session started by greetd is the issue.

=== Hide network folder icon on LXQt desktop ===
Date: 2025-11-09

The network folder icon is unnecessary. Removed it from the PCManFM-Qt configuration.

=== Fix VISUAL environment variable not being set by usability-misc profile.d script ===
Date: 2025-11-09

The script checked to make sure it wasn't running in a TTY before changing the value of the VISUAL env var. This is no longer correct because the Wayland session itself runs in a TTY (or at least appears to). Removed the TTY check, now the variable is set correctly.

=== Install vm-config-dist on all Kicksecure and Whonix installations ===
Date: 2025-11-09

vm-config-dist is now able to be installed on all platforms, making ISOs work better on virtual machines and making physical-to-virtual and virtual-to-physical migrations theoretically easier. Moved it to the dist-general-cli metapackage.

=== Terminate VBoxDRMClient during shutdown ===
Date: 2025-11-09

Added code to security-misc that terminates VBoxDRMClient when the system is shut down. This fixes the /tmp unmount issue (which is already resolved upstream, we may be able to remove this code soon hopefully). At around this time I discovered my bug report against VirtualBox for this issue was a duplicate of an already solved issue.

=== Use PAM to enforce login control in lieu of password locks, simplify login security table ===
Date: 2025-11-09

Created a new script to use with pam_exec.so for preventing sysmaint login in user mode, non-sysmaint login in sysmaint session, and unsafe passwordless login. Simplified the login security table code since the PAM change made it no longer necessary to special-case the sysmaint account when looking at password status.

=== Refactor sdwdate-gui IPC data parsing and config file parsing code ===
Date: 2025-11-09

While applying a ChatGPT-created bugfix, I discovered that the config file parsing code was inconsistent, contained redundancies, and could be mostly replaced with tomllib. The IPC data parsing code was also inconsistent and contained redundancies. Refactored the code to unify multiple implementations of the same code, and rely on tomllib for most of the hard work of config file parsing.

=== Comment on application replacement suggestions on Whonix forum ===
Date: 2025-11-09

Suggested that we drop a PDF viewer in Whonix, in favor of the one built into Tor Browser. Also pointed out that the use of GNOME apps in Kicksecure and Whonix isn't much of a concern when it comes to number of additional dependencies installed, and mentioned that the use of Flameshot at the moment is because Screengrab doesn't work with Wayland in Debian Trixie. We will want to switch to Screengrab later.

== 2025-11-08 ==
=== Fix AppArmor issues in kloak, silence a spurious journal warning in systemcheck output ===
Date: 2025-11-08

Most of the warnings systemcheck printed from the journal on a Kicksecure 18 system turned out to be because of AppArmor configuration problems in kloak. Resolved those issues. Also silenced a weird "success" warning related to disk encryption coming from the audit logs.

=== Debug, file bug report for /tmp unmount failure caused by VBoxDRMClient ===
Date: 2025-11-08

Discovered that /tmp was failing to unmount at shutdown because of VBoxDRMClient. Filed a bug report (which turned out to be a duplicate despite having searched for bug reports first, because I failed to check closed reports).

=== Investigate fixing sgdisk warning about partition end alignment and disk encryption ===
Date: 2025-11-08

Discovered that the issue is a mismatch in how sgdisk and parted think partitions should be aligned. Sent an email to the GPT fdisk general mailing list to ask about what the best way of resolving this would be.

=== Make errors in sysmaint-boot more obvious ===
Date: 2025-11-08

Made it so that if sysmaint-boot errors out, the error messages it displays are printed to the console, and remain visible for five seconds before boot proceeds.

=== Hide virtual cursor image by default in kloak ===
Date: 2025-11-08

Changed the default cursor color in kloak from opaque red to fully transparent. Also added code to kloak to avoid drawing a virtual cursor at all (even a transparent one) if the cursor color is entirely transparent.

== 2025-11-07 ==
=== Apply more Qubes memory usage reduction changes ===
Date: 2025-11-07

Made usbguard not run when it doesn't have to. Suggested to Patrick that we merge in the changes to sdwdate-gui-client before the initial release of Kicksecure/Whonix 18.

=== Merge in ChatGPT Codex suggestions for live-hardener submount handling ===
Date: 2025-11-07

Codex caught a bug in live-hardener (still using plain prefix matching for detecting submounts, which is prone to false-positives). Merged in the fix and regression test enhancements.

== 2025-11-06 ==
=== Bugfix, improve live-hardener ===
Date: 2025-11-06

Found several issues with live-hardener (mainly related to attempting to overlay or remount filesystems that shouldn't be modified, and incorrectly handling submounts). Refactored the script substantially to fix these issues and improve regression test coverage.

=== Find and fix root cause of live mode failures ===
Date: 2025-11-06

Patrick and I determined that the live mode issues were caused by the <code>dmsquash-live</code> module not being included in the initramfs after the first time the initramfs is regenerated. Added configuration to grub-live to ensure that module is always included.

== 2025-11-05 ==
=== Continued debugging of live mode failures ===
Date: 2025-11-05

Researched how Dracut live mode internally, and discussed more possibilities for why things are breaking with Patrick. Still haven't discovered the root cause of the issue.

=== Create batch files for managing Hyper-V under Windows ===
Date: 2025-11-05

Fleshed out a script started yesterday that disables several Hyper-V related security features. Also fleshed out a companion script that re-enables several features that are powered by Hyper-V, and created a script that displays info related to Hyper-V-related feature configuration.

== 2025-11-04 ==
=== Investigate disabling Hyper-V under Windows ===
Date: 2025-11-04

Worked with Patrick to discover the steps needed to disable Hyper-V on Windows 11. This should allow VirtualBox to run at full speed.

=== Make it more obvious when a Qubes HVM doesn't have enough RAM to work with the Kicksecure ISO ===
Date: 2025-11-04

Bumped up the RAM limited in rads and adjusted how the low RAM message from rads is displayed so that users are more likely to notice it.

=== Test Kicksecure ISO for live mode issues ===
Date: 2025-11-04

Attempted to reproduce problems with live mode under Kicksecure on VirtualBox. Was not able to reproduce the problems, even when working with Patrick to figure out the root cause.

=== Test Whonix 18 for disk corruption on Manjaro ===
Date: 2025-11-04

Installed a pair of Whonix VMs in Manjaro using KVM. Was not able to reproduce the disk corruption issue with them. Documented a possible workaround for the issue on Whonix 17, considering the issue solved enough.

== 2025-11-03 ==
=== Debug Whonix disk corruption bug under Fedora and Manjaro, investigate Windows privilege elevation ===
Date: 2025-11-03

Was not able to reproduce the disk corruption issue on Fedora 42 KDE. Was able to reproduce the issue once on Manjaro KDE, but could not reliably reproduce it thereafter. Took some notes about the issue and left a suggestion for how to possibly work around it. Also researched how to handle user account control and privilege elevation under Windows so that Whonix-Installer can disable Hyper-V automatically during the installation.

=== More systemcheck gripe fixing under Qubes OS ===
Date: 2025-11-03

Discovered the root cause of one one of the systemcheck gripes (specifically the oen related to systemctl and D-Bus user bus access). Filed a bug report against Qubes OS for the issue.

== 2025-11-02 ==
=== policyrcd-script-zg2 feature request for drop-in config directory ===
Date: 2025-11-02

Asked the upstream developers of policyrcd-script-zg2 if they'd accept a drop-in config directory feature contribution.

=== Fix some systemcheck gripes under Qubes OS ===
Date: 2025-11-02

Patrick and Marek both were seeing some systemcheck messages that should not have been appearing. Fixed the root cause of several of them, silenced other false-positives. Haven't finished debugging all of them yet, but most of them are fixed.

=== Implement better power management defaults ===
Date: 2025-11-02

Added improved power management configuration to desktop-config-dist. The new configuration should be reasonably usable while providing better security than LXQt's default configuration (by virtue of locking the screen rather than suspending to avoid a race condition that could leave the screen unlocked).

=== Make Livecheck only show one information window at once ===
Date: 2025-11-02

A user could open a theoretically unlimited number of Livecheck windows at the same time previously. Added some code so that the user could not open a new Livecheck window until they closed the old one.

=== Reload labwc keyboard layout when using set-system-keymap ===
Date: 2025-11-02

Added code to the <code>set-keyboard-layout.sh</code> library that reloads labwc configuration after setting the system-wide keyboard layout.

=== Fix text encoding issue caused by improper locale setup on Kicksecure VM images ===
Date: 2025-11-02

Flatpak was outputting question marks in place of Unicode in some instances. Discovered this was because of improper locale configuration, determined why the locale was not being configured properly, and created a fix.

=== Fix sdwdate-gui issues when sys-whonix is booted in sysmaint session ===
Date: 2025-11-02

We were hardcoding a UID in the sdwdate-gui proxy helper script. Fixed this so that it determines the correct UID based on the value of the <code>/default-user</code> key in qubesdb. Also fixed an issue where the sdwdate-gui icon would get stuck on the wrong icon in some instances.

=== Reproduce, file bug report for LXQt sleep + screen locking issue ===
Date: 2025-11-02

Successfully reproduced the screen lock timing issue on Arch Linux, and filed a bug report about it. Turns out to be a race condition between putting the system to sleep and locking the screen.

=== Test new disallowed-test in systemcheck on a newly build Kicksecure KVM image ===
Date: 2025-11-02

Previously the AppArmor "disallowed-test" failed for me in a KVM virtual machine. Built a new VM from scratch, discovered that the issue does not occur there.

== 2025-11-01 ==
=== Try to reproduce LXQt sleep + screen locking issue on Arch ===
Date: 2025-11-01

On Kicksecure, LXQt would sometimes put the system into sleep before locking the screen all the way. Attempted but failed to reproduce the issue on Arch Linux, made some notes for future debugging.

=== Investigate user-sysmaint-split vs policyrcd-script-zg2 ===
Date: 2025-11-01

Looked at how policyrcd-script-zg2 works and how it interacts with user-sysmaint-split. Suggested that we remove it from our dependencies.

=== Document flatpak authorization hardening in security-misc ===
Date: 2025-11-01

Added documentation to the README about the Flatpak authorization hardening added to security-misc.

=== Make vm-config-dist installable on all platforms ===
Date: 2025-11-01

Removed some deprecated code from vm-config-dist, and added needed checks to make sure it could be installed on physical systems and virtual systems other than VBox and KVM. This should make it easier to move Kicksecure images between hypervisors, and allow us to ship vm-config-dist on the Kicksecure ISO to improve the experience for users who use the ISO in a virtual machine.

== 2025-10-31 ==
=== Research, start discussion on power management config for physical Kicksecure systems ===
Date: 2025-10-31

Went through all of the settings supported by LXQt for power management, documented advantages and disadvantages of each one from a security and usability perspective, and created a recommendation for settings we should use by default. Awaiting feedback from forums.

=== Investigate, harden Flatpak security rules for app installation and management ===
Date: 2025-10-31

Patrick discovered that one could install a Flatpak system-wide even when running in a user session and logged into account <code>user</code>. Discovered why this was the case, and implemented changes to Flatpak's configuration to prevent this and require authentication for most Flatpak-related operations.

=== Fix fakeroot-caused systemctl errors in dnf-3 uwt wrapper, silence unnecessary warnings ===
Date: 2025-10-31

alimj pointed out that the issue with Qubes updates showing "Operation not permitted" errors was because of fakeroot. Determined that it was possible to use <code>privleap</code> to work around the issue. Implemented the fix, and also silenced some warnings that weren't important for the user to see.

== 2025-10-30 ==
=== Start preparing vm-config-dist to be suitable for installation on all platforms ===
Date: 2025-10-30

vm-config-dist has some things that are useful for Qubes, and we want it installed on the Kicksecure ISO so that if people boot the ISO in a virtual machine, they get the benefits it provides in that VM. Started work to make vm-config-dist able to be installed without issues on all supported platforms.

=== Diagnose, mask cast-qual compilation warning in kloak ===
Date: 2025-10-30

The way in which libwayland-client works ends up with a "const" qualifier being discarded when attaching a listener to an interface. This issue is present in the library itself and also in code automatically generated by wayland-scanner. Determined why it was happening, decided it was likely benign, and added some GCC pragmas to kloak to silence cast-qual warnings from two autogenerated header files.

=== Enable systemd-journald audit transport to fix apparmor-info ===
Date: 2025-10-30

apparmor-info wasn't working because the systemd-journald audit transport was not enabled and apparmor-info attempts to scan messages from that transport. Determined how to enable it, and did so. Needs an image rebuild to test and make sure it fully works, which has not been done yet.

=== Review, adjust backlight tool changes ===
Date: 2025-10-30

Reviewed some changes to backlight-tool-dist that Patrick made, made some changes to strings displayed in the UI.

=== Fix graphical relog in sysmaint sessions ===
Date: 2025-10-30

We weren't taking into account the possibility that someone might log out of a graphical sysmaint session without rebooting. However, one can cause what looks like a relog by running <code>sudo systemctl restart greetd</code>. Upon logging back in, a black screen would be displayed. Determined why the issue was occurring and fixed it.

=== Finish screen locking enhancements ===
Date: 2025-10-30

Added the needed code to disable screen locking under VMs via vm-config-dist. Tested it, appears to work.

== 2025-10-29 ==
=== Improve screen locking experience ===
Date: 2025-10-29

Unified all screen locking controls in helper-scripts' <code>lock-screen</code> script. Added better notifications, LXQt integration, and a much better screen lock notification image.

=== Try to debug dnf-3 issues in Qubes-Whonix-Gateway ===
Date: 2025-10-29

Marek saw an OpenQA failure with the new dnf-3 wrapper. Could not determine what was wrong with it in debugging, may have been a transient issue or related to the fact that we had two different argument injection mechanisms working at the same time. Did some research, shared results in chat, and left things unchanged in this area for now.

== 2025-10-28 ==
=== Add missing systemd units to sysmaint-boot.target ===
Date: 2025-10-28

Found several units we weren't starting in sysmaint session that we were supposed to be, and added them to the "Wants" of sysmaint-boot.target.

=== Write dnf-3 wrapper for dom0 updates over Whonix-Gateway ===
Date: 2025-10-28

Created a proxy argument injection wrapper for dnf-3 to replace the qubes-specific proxy argument injection solution. Mostly works, but Marek spotted issues with it in OpenQA, which I'll need to work on.

=== Review suggested changes tor-control-panel for separating it from anon-connection-wizard ===
Date: 2025-10-28

Looked at and commented on some code changes submtited by troubador. Also mentioned an issue with the "new identity" button.

=== mediawiki-shell static analysis ===
Date: 2025-10-28

Used static analysis tools on mediawiki-shell (BashSupport Pro and Shellcheck) to find and fix a few more issues.

== 2025-10-27 ==
=== Continued review of mediawiki-shell refactoring ===
Date: 2025-10-27

Finished reading through and polishing the mediawiki-shell refactoring work from Ben. Need to still run some analysis tools on it, but it's close to done.

=== Comment on qca-addon-whonix PR from Ben (default dvm set issue) ===
Date: 2025-10-27

Continuing discussion on how to best set the default DispVM for Whonix templates.

== 2025-10-26 ==
=== Finish initial port of sdwdate-gui-client to asyncio and pyinotify ===
Date: 2025-10-26

Originally we were using Qt for sdwdate-gui-client, since it allowed convenient handling of both network and file change notifications. This required more memory than using lower-level frameworks however. Rewrote sdwdate-gui-client to use asyncio and pyinotify instead. The initial port seems to work but needs more review and testing.

=== Begin review of mediawiki-shell refactoring ===
Date: 2025-10-26

Got through a significant portion of the review work, still more than half left to do. Made adjustments as I went.

=== Set resolution of all virtual displays to 1920x1080 if dynamic resize helpers are not available ===
Date: 2025-10-26

If spice-vdagentd is not present and running under KVM, or if VBoxDRMClient is not present and running under VirtualBox, wl_resize_watcher will set the size of all displays on the system to 1920x1080 to avoid issues with the screens being too small.

=== Split set-system-keymap and set-console-keymap ===
Date: 2025-10-26

Created a new wrapper "set-console-keymap" for just setting the console keyboard layout separately from the labwc layout, and made "set-system-keymap" a wrapper around both set-console-keymap and set-labwc-keymap.

=== Fix policy-rc.d deletion issue in qubes-builder-debian ===
Date: 2025-10-26

qubes-builder-debian was not using update-alternatives when installing a policy-rc.d file to prevent service startup during package installation. As a result, our policy-rc.d shipped in user-sysmaint-split was being clobbered. Created a PR to Qubes to fix this, which was merged.

=== Document dnf workaround for libcurl dom0 updates over onion repos on Qubes ===
Date :2025-10-26

Documented the new workaround added to Qubes for allowing DNF to fetch dom0 updates from an onion repo via Whonix-Gateway, and why it was necessary.

== 2025-10-25 ==
=== Reduce Qubes-Whonix memory requirements ===
Date: 2025-10-25

Identified a number of areas where memory usage could be reduced, some that were Qubes-specific and some which weren't, and implemented many of them. (A rewrite of sdwdate_gui_server.py to use asyncio and pyinotify is still in progress.)

=== Add set-system-keymap button to sysmaint-panel ===
Date: 2025-10-25

There is now a button in sysmaint-panel that will launch the system-wide keyboard configuration application.

== 2025-10-24 ==
=== Port live-config-dist to the new set-system-keymap mechanism ===
Date: 2025-10-24

The default keyboard layouts set by Calamares are now set via set-system-keymap rather than a custom, Calamares-specific mechanism.

=== Downgrade hard failures to warnings in most places in wlr_resize_watcher ===
Date: 2025-10-24

Made it so that if an error occurs that will probably render wlr_resize_watcher useless but won't completely break it, it will remain running just in case it can be useful.

=== Test dom0 updates over Whonix-Gateway ===
Date: 2025-10-24

Verified that dom0 is able to use Whonix-Gateway as an update proxy successfully.

=== Actually remove incorrect volume widget from Whonix-Gateway sysmaint waybar ===
Date: 2025-10-24

The initial removal attempt didn't quite work because the pulseaudio widget was still being added, it just wasn't being configured specially in Whonix-Gateway anymore. Fully removed the widget from the panel.

== 2025-10-23 ==
=== File Debian systemd package bug report for enabling X11 keyboard layout changes via systemd-localed ===
Date: 2025-10-23

Filed a bug asking for the X11 keymap setting functionality disabled by systemd-localed-read-only.conf to be enabled somehow. Offered to work on fixing it if possible.

=== Stop installing unnecessary firmware in Qubes OS Kicksecure template ===
Date: 2025-10-23

We were installing GPU and audio card firmware in Kicksecure 18, which is unnecessary (only network and ''maybe'' USB firmware is needed there for use as sys-net and sys-usb). Split out network firmware into a separate metapackage for Kicksecure on Qubes OS to use.

=== Rename sdwdate.Connect(Check) to sdwdate-gui.Connect(Check) ===
Date: 2025-10-23

The old naming was annoying and the Qubes team was fine with me renaming this. Applied the name change to qubes-core-admin-addon-whonix and sdwdate-gui.

=== Review whether anything is left to do on fixing sdwdate-gui qrexec denied messages ===
Date: 2025-10-23

Didn't see anything left to do here, the bug appears to be fixed.

=== Remove incorrect volume widget from Whonix-Gateway sysmaint waybar ===
Date: 2025-10-23

Whonix-Gateway doesn't have an audio device, so the volume widget was useless and just appeared as a random yellow bar in the middle of Waybar. Removed it by splitting the Waybar configuration out of desktop-config-dist and putting it in kicksecure, anon-gw, and anon-ws base files packages, then removing the volume widget config from anon-gw-base-files.

=== Fix systemcheck error trap to return non-zero if a non-zero return code was trapped ===
Date: 2025-10-23

Sourcing systemcheck configuration that was invalid was properly resulting in systemcheck aborting, but was not resulting in systemcheck exiting with a non-zero exit code. Found and implemented a solution.

=== Add hypervisor helper checks to wlr_resize_watcher ===
Date: 2025-10-23

Made it so that wlr_resize_watcher will print an error message and exit if VBoxDRMClient (on VirtualBox) or spice-vdagentd (on KVM) is missing or not running.

=== Debug sdwdate permissions problems on upgraded sys-whonix AppVM ===
Date: 2025-10-23

When switching sys-whonix from a Whonix-Gateway 17 template to a Whonix-Gateway 18 one, the UID/GID on /var/lib/sdwdate was incorrect, since the UID/GID for <code>sdwdate:sdwdate</code> in the Whonix-Gateway 17 template were different than the UID/GID for the Whonix-Gateway 18 template. A tmpfiles.d configuration file should have been fixing this, but it was failing to do so because systemd-tmpfiles-setup.service was starting before qubes-bind-dirs.service. Added an ordering dependency to qubes-bind-dirs.service to fix this and submitted the change as a PR to Qubes.

=== Fix "ignoring exit-on-service-eof=true" warning in Whonix-Gateway 18 on Qubes ===
Date: 2025-10-23

There was a config file we were supposed to override when we fixed the updates proxy startup timing issue. Added an override for that file.

== 2025-10-22 ==
=== Fix up browser-choice Tor Browser plugin, install button availability ===
Date: 2025-10-22

The Tor Browser plugin for browser-choice now simply installs the Tor Browser package and then instructs the user on how to download Tor Browser separately. If a browser is already considered to be installed by browser-choice, the "install" option is grayed out.

=== Remove xscreensaver from release-upgraded systems ===
Date: 2025-10-22

Now that we've ported to Wayland, we don't support xscreensaver anymore, and Qubes doesn't need it even though it uses X still. It causes the screen to go black without warning under Wayland, removing it should prevent that going forward.

=== Remove hardcoded "sleep 10" from Tor startup, replace with IPv6 address listener ===
Date: 2025-10-22

Created a script that listens for the appearance of a hardcoded internal IPv6 address, and allows Tor to start once that address appears (or 10 seconds passes, whichever comes first).

=== Finish changing anon-connection-wizard, tor-control-panel to using privleap for privileged operations ===
Date: 2025-10-22

Code is untested but should work to make these tools functional on Whonix-Gateway with user-sysmaint-split installed.

=== Testing, further polish on keyboard layout code ===
Date: 2025-10-22

Ran through a detailed test plan for the keyboard layout code to ensure it functioned properly in a wide range of situations. Created some bugfixes and merged in a bugfix from Codex.

== 2025-10-21 ==
=== Mostly finish keyboard layout code ===
Date: 2025-10-21

Finished the interactive UI for the keyboard layout customizer, added support for changing the system-wide keyboard layout (including the console layout), and created a thorough test plan for the code (yet to be ran through).

== 2025-10-20 ==
=== Work on polishing keyboard layout change code ===
Date: 2025-10-20

Started creating an interactive UI for changing the keyboard layout under labwc. Also did research into how to configure the system-wide keyboard layout for both Wayland and the console.

=== Fix greetd login failure hang ===
Date: 2025-10-20

greetd would hang on a black screen if the user failed to authenticate properly the first time. Figured out why this was happening and fixed it.

=== Fix several post-upgrade desktop environment issues ===
Date: 2025-10-20

Fixed greetd failing to be enabled, fixed an autologinchange bug that was corrupting a greetd configuration file, and reviewed a change make to sysmaint-boot to ensure that Wayland was the default for the sysmaint session.

== 2025-10-19 ==
=== Research, create qubes-devel thread for avoiding AppVM and NetVM/DispVM mismatches ===
Date: 2025-10-19

Posted a mailing list thread with an idea about how to keep a Qubes ApPVM from being set to use a DispVM or NetVM that is unsafe for that specific VM.

=== Create screen brightness tool for Kicksecure 18 ===
Date: 2025-10-19

The tool uses privleap rather than pkexec as the privilege escalation framework. The security-sensitive components of the code are written in Bash and validate their input thoroughly, so this should be secure.

=== Finish dynamic display resize helper for labwc on VirtualBox and KVM ===
Date: 2025-10-19

Runs as a background process, detects changes in display size and reconfigures the compositor as appropriate when those changes are detected. Tested with both single and multiple screens in VirtualBox, and a single screen in KVM.

=== Reply to kernel lockdown mode Github issue, forum posts ===
Date: 2025-10-19

Replied to forum posts and a Github issue related to enabling kernel lockdown mode. Couldn't think of a scenario where kernel lockdown mode would be substantially useful, but left some notes.

== 2025-10-18 ==
=== Start development of dynamic resolution handler for labwc ===
Date: 2025-10-18

Finished doing the research needed to create a background process that will listen for display resolution change events and resize labwc displays accordingly. This will allow dynamic resolution resizing to work in both VirtualBox and KVM.

=== Investigate display brightness concerns for LXQt on Kicksecure 18 ===
Date: 2025-10-18

pkexec is not executable from a user session, which makes it hard for the LXQt backlight helper to be usable. After some discussion, we've decided to create our own backlight tool for this. Because privleap only streams stdio in one direction, this will need to have a dedicated helper created for it; to avoid needing SUID root and to prevent a persistent daemon from having to run in the background, we can probably use systemd socket activation or D-Bus activation here.

=== Fix repository-dist-wizard UI element overlap issue ===
Date: 2025-10-18

Changed repository-dist-wizard to use layouts rather than manual UI element positioning to avoid overlap issues (these previously resulted in inability to click some buttons).

=== Finish adding user-sysmaint-split to Whonix-Gateway ===
Date: 2025-10-18

Tested a freshly built pair of Whonix 18 KVM images, verified that user-sysmaint-split was present in Whonix-Gateway and functional.

== 2025-10-17 ==
=== Look into further work needed for Kicksecure 18 on Qubes OS, enable user-sysmaint-split on Whonix-Gateway ===
Date: 2025-10-17

Determined that we do not need to do anything about qubes-core-admin-addon-kicksecure's installation on Qubes OS R4.3, as Marek is already planning on ensuring it is installed by default. Changed our code to place user-sysmaint-split on Whonix-Gateway by default after some discussion of attack scenarios with Patrick (not all code changes pushed yet, still testing).

=== Further review on ben-grande's qubes-core-admin-addon-whonix refactor ===
Date: 2025-10-17

Continuing to discuss some implementation details. Once this is done, we should backport this to R4.2 to fix the anonymity bypass via DispVMs in standalones and templates bug.

=== Change passwordless-root to be ephemeral by default in Qubes AppVMs ===
Date: 2025-10-17

Patrick brought up the fact that the <code>passwordless-root</code> command is permanent even in Qubes OS AppVMs, so the unrestricted admin mode fix created previously didn't work as expected. Changed <code>passwordless-root</code> to be ephemeral by default on Qubes, and provide a command-line option for enabling persistence.

== 2025-10-16 ==
=== Review, attempt to send stardict removal request ===
Date: 2025-10-16

Attempted to send the Stardict email, however GMail refused to allow me to send it for some reason. Reviewed email content, removed anything that might look suspicious to GMail and decided to leave it alone for a bit so that any anti-spam measures would have time to calm down.

=== Debug miscellaneous Qubes-related Kicksecure and Whonix issues ===
Date: 2025-10-16

Verified that updates, release upgrades, and user-sysmaint-split work as expected. Determined what was wrong with Kicksecure sys-whonix communication and discussed possible fixes with Patrick.

== 2025-10-15 ==
=== Investigate, write draft email requesting the removal of Stardict from Debian's main repo ===
Date: 2025-10-15

Determined that Debian 12 was still vulnerable to a Stardict CVE that leaked arbitrary clipboard contents. Prepared an email detailing Stardict's history and requesting that the package be demoted to contrib, and shared it with Patrick. This has not yet been submitted to Debian upstream.

=== Investigate missing display resize events in VirtualBox ===
Date: 2025-10-15

Discovered that VirtualBox VMs were not reliably receiving signals when the display is resized, even with VirtualBox guest additions installed. Determined this was the result of a missing executable, <code>VBoxDRMClient</code>. Found a fix and emailed it to the Debian VirtualBox maintainers.

=== Miscellaneous review tasks ===
Date: 2025-10-15

Replied to several PRs and issues. Merged some code from raja-grewal.

=== Test, fix USBGuard in Qubes ===
Date: 2025-10-15

Discovered that USBGuard mostly worked in Qubes, but not entirely. Created fixes for the issues discovered.

== 2025-10-14 ==
=== Implement workaround for approx cache outdated repo metadata in derivative-maker ===
Date: 2025-10-14

Created and tested a fix for approx's occasional failure to download updated package metadata in our build system.

=== Find, fix core issue with Qubes environment fix patch ===
Date: 2025-10-14

The issue with the patch to qubes-gui-runuser was finally found; there was a race condition between systemd attempting to terminate qubes-gui-agent.service and qubes-gui-runuser starting the GUI session. Fixed the issue, the PR now works as expected.

== 2025-10-13 ==
=== Debug approx cache issues, comment on security-related kernel parameters issue on Github ===
Date: 2025-10-13

Debugged and reported a bug about approx's caching issues (it looks like there's possibly a timer not being obeyed). Also did some research on kernel command line maximum length and commented to raja-grewal on a Github issue about the use of <code>mitigations=auto,nosmt</code>.

=== Rework Qubes environment fix patch ===
Date: 2025-10-13

Discarded the original C code changes for importing the environment in qubes-gui-runuser, and attempted to do this in /etc/profile.d instead. Unfortunately this seems to have now caused a sys-gui issue that will need further investigation.

== 2025-10-12 ==
=== Research what is needed to get port from gpg to sequoia-pgp ===
Date: 2025-10-12

Wrote down some suggestions, created a file for tracking progress, and discussed next steps with Patrick.

=== Create Tor version check and update script ===
Date: 2025-10-12

Made a Python script that detects if the version of Tor available from torproject.org is newer than the version in the Kicksecure repositories, and downloads the update into the repository if so.

=== More debugging of Qubes VM shutdown issues with environment fix patch ===
Date: 2025-10-12

Found the root cause of the issue, but I'm still not sure why the code in question is causing the issue. Marek suggested some workarounds which I'll be testing soon.

=== Isolate log filtering code in systemcheck from normal users ===
Date: 2025-10-12

Split out most of the log processing into a new script that runs as user <code>systemcheck</code>. That script can be called by standard users via leaprun, and it in turn can read the system logs, but standard users cannot read system logs. This should prevent access to unhashed kernel pointers, at least through the system logs.

=== Fix sysmaint manual login and related greetd issues ===
Date: 2025-10-12

Patrick discovered that one could log into a full desktop session with the sysmaint user by restarting greetd. Determined why this was happening and fixed it. Also fixed greetd startup when sysmaint autologin is disabled.

=== Fix volume indicator in sysmaint session changing color on hover ===
Date: 2025-10-12

The default Waybar configuration on which our config was based had an unnecessary styling rule that made the volume indicator change color on hover. Removed the rule.

== 2025-10-11 ==
=== Rework keyboard layout handling in Calamares ===
Date: 2025-10-11

Adjusted our keyboard layout helper code so that the keyboard layout was properly set in /etc/default/keyboard and in the labwc compositor. The new code should now support locales that use multiple keyboard layouts by default, and should also work with keyboard layout variants.

=== Further discussion of stalled Qubes OS template shutdown issue ===
Date: 2025-10-11

Determined that the environment variables we needed were available over Varlink. Discussed with Marek whether a script-based solution or the use of Varlink would be worthwhile for fixing the issue.

== 2025-10-10 ==
=== Debug locked-up shutdown of Qubes OS VMs after adding the environment import patch ===
Date: 2025-10-10

Marek discovered that the systemd environment import patch to qubes-gui-runuser resulted in shutdown getting stalled for a minute and a half in some instances. Attempted to determine why, and discussed possible solutions.

=== Fix flickering in Qt apps on Qubes OS triggered by event buffering ===
Date: 2025-10-10

Added some code to qubes-gui-daemon to allow instant passthrough of a few X messages to fix the flicker issue.

=== Add back VirtualBox guest utilities to Kicksecure ISO ===
Date: 2025-10-10

Determined why the VirtualBox guest utilities weren't being installed on the Kicksecure ISO in the first place, fixed the issue that kept them from being installable, and added them. Verified they were present on a newly built ISO.

=== Add display settings button to sysmaint-panel ===
Date: 2025-10-10

You can now launch wdisplays by clicking the display settings button in sysmaint-panel.

== 2025-10-09 ==
=== Discuss solutions for Calamares keyboard layout set failure ===
Date: 2025-10-09

Calamares' keyboard layout settings currently do not work under Wayland due to Debian disabling the mechanism Calamres uses to make keyboard layout changes. Described the issue to Patrick and mentioned possible solutions.

=== Ensure Calamares language setup worked as expected ===
Date: 2025-10-09

Both GUI and CLI modes of user and sysmaint sessions obeyed Calamares' language settings.

=== Investigate debian.sources only being readable by root ===
Date: 2025-10-09

Could not reproduce issue.

=== Create setxkbmap alternative for labwc ===
Date: 2025-10-09

Created set-labwc-keymap, for setting the keyboard layout with labwc. Can make both temporary and persistent changes. Supports keymap options and variants.

=== Research making left click bring up sdwdate-gui menu ===
Date: 2025-10-09

Determined that this was not possible at the moment and recorded why in the corresponding task. Changed a UI string so that users know to right-click on the icon.

=== Add LXQt config and on-screen keyboard to sysmaint-panel ===
Date: 2025-10-09

Added buttons to sysmaint-panel for toggling the on-screen keyboard and launching lxqt-config.

== 2025-10-08 ==
=== Fix Qubes Update failure on Whonix templates ===
Date: 2025-10-08

The ten-second wait added to the Tor startup process was causing the updater to regularly fail on Whonix templates. Changed qubes.UpdatesProxy in Whonix from a symlink to a script that would wait to allow updates to begin until Tor was up-and-running in sys-whonix.

=== Add an on-screen keyboard to Kicksecure and Whonix ===
Date: 2025-10-08

After much research, decided to choose wvkbd as the keyboard to ship. Created a script and desktop files for starting and stopping it.

=== Fix bindp compilation warning caused by incorrect postinst compilation method ===
Date: 2025-10-08

We were embedding a gcc command into the postinst script rather than using the compilation procedure in bindp's Makefile. Fixed this.

=== Finish preparing qubes-gui-runuser.c env var code for review ===
Date: 2025-10-08

Did more testing, fixed remaining TODOs. Pushed the latest version of the env var code and marked it as ready for review.

== 2025-10-07 ==
=== Polish qubes-gui-runuser.c env var code, research slab_debug security concerns ===
Date: 2025-10-07

Fixed several bugs in the draft implementation of the environment variable import code from yesterday, and got the new implementation to work in a Debian 13 Xfce template. Also did substantial research into security concerns around the <code>slab_debug</code> kernel parameter, and how to mitigate them best. Requested that the Debian kernel team backport a new pointer hashing boot parameter from kernel 6.17 into the Trixie stable kernel.

== 2025-10-06 ==
=== Create draft implementation for importing env vars from systemd in qubes-gui-runuser.c ===
Date: 2025-10-06

The preliminary fix for the environment variable bug in Qubes was insufficient because it meant now environment variables from systemd were being clobbered with those from /etc/profile.d. The intended behavior was that environment variables from systemd should be imported, then augmented by /etc/profile.d. Marek suggested implementing code to do this in qubes-gui-runuser.c, using D-Bus to communicate with systemd to get the environment variables and export them into the session. I created an initial rough draft of this implementation and posted it for initial review.

=== Attempt to debug kloak-related systemd unit ordering cycle ===
Date: 2025-10-06

Installed an older version of kloak into a Debian 13 GNOME VM. Could not reproduce issue, waiting on more info.

=== Remove sanitizers from compiled code ===
Date: 2025-10-06

We were enabling sanitizers such as AddressSanitizer and UndefinedBehaviorSanitizer as a hardening measure, but it turns out this actually made programs less secure and that these sanitizers are not intended for production code. Disabled C sanitizers on all of our C code.

== 2025-10-05 ==
=== Investigate Qubes input event buffering flicker bug with Qt ===
Date: 2025-10-05

Previously I discovered that Qt apps had some odd flickering behavior with some menus when input event buffering was enabled. Attempted to find the root cause, the cause hasn't been identified for certain but a possible problem has been identified. Discussed it with Marek briefly.

=== Test socat's suitability for use as a DNS proxy in sys-net ===
Date: 2025-10-05

Got socat working so that IPv6 DNS on an IPv6 network worked. Discussed the suitability of this solution with Marek.

=== Debug, create preliminary fix for Qubes-Whonix file manager launch bug ===
Date: 2025-10-05

Marek discovered that the default file manager in Whonix 18 was Catfish, when it should be PCManFM-Qt. After much study it was discovered this was the result of an outdated xdg override combined with an environment variable loading issue in Qubes OS itself. Filed a bug report for the environment variable load issue, discussed possible ways of fixing it with Marek, and submitted a PR. A different approach than the one provided in the PR will need to be used in the long run.

== 2025-10-04 ==
=== Test Qubes OS IPv6 DNS PRs ===
Date: 2025-10-04

Closely read through, polished, and tested the IPv6 DNS PRs from 3nprob. Unfortunately the approach being used by them is insufficient on its own due to some routers exposing a DNS server on a link-local IPv6 address, which cannot be properly targetted by a DNAT rule. Some sort of manual forwarding using a tool such as socat will likely be required.

= Hans =

== Current sprint ==

=== Sprint 2026-04-27 to ... ===

* 2026-05-08
** Checking Templates for Multiwiki compatibility
** Researching + inventing best way for widget comments of blocks + suggestions
** documentation for DonatePageContent
* 2026-05-07
** Claude Review 3 Merge conflicts
** Github link template new version review
*** new wikitests
*** analysis + suggestions
*** ai rewrite analysis and more suggestions
** mediawiki css ext claude merge detailed review
*** suggestions + write prompt
** IconSet feature upgrade maybe
*** wikitests + documentation
** espacing wiki pages ai review
* 2026-05-06
** Claude Review 2 with detailed commentary
** EditorAddNewEditButtons better access pattern for template object
** Review and delete old pages
** Deletion of irrelevant Mediawiki or Widget pages
** CSS Hide protected icon
** Github link template claude version 2 review
** CSS fix donation panel alignment
** Wikibook Index weird wrapping analysis and suggestions
** add bluesky link and logo to footers
* 2026-05-05
** wiki edit page scrolling bug deep analysis + fix
** EditorAddNewEditButtons
*** add indent right and left buttons
*** select indent button
*** cleanup whole file
*** documentation
** Donation panel documentation update
* 2026-05-04
** FlyInNotification optimization and documentation
** Html Donation Panel Crypto [pre] crypto address bug fix
** MediaWiki:Sitenotice end year banner integrated, in-place documentation + wiki documentation
** Widget SiteNoticeBanner null checks, parameter reduction, improved readability
** Template:Header Donation Panel CSS for SitenoticeBannerEndOfYear integrated
** Template:SitenoticeBannerEndOfYear right side designed, left side placeholder, parameter optimizations (now minimal), wiki documentation
** Template:Sitenotice id - year end check mechanism and in-place documentation
* 2026-05-01
** Reposition augmented editor icons due to new mediawiki
** Sanatizer Bypass
*** Research analysis suggestion
*** documentation on new method for HTML templates for code modularization
** new Template SitenoticeBannerEndOfYear + only visible on fly in testpage
** Widget SitenoticeBanner updated for new mechanics
* 2026-04-30
** sitenotice-banner
*** add specialtype for yearend
*** restructure widget completely
*** add month info and crypto info to template
*** december detect and crypto separation in widget
*** mandatory params warning in widget
* 2026-04-29
** Table Expand Skin active detection and script deactivation based on that
** iframe analysis bug due to ai fixes + suggestions
** mobile table select bug analysis + suggestions + simple solution
** FlyInNotification analysis + optional settings feature + december settings + new expires method + refactoring + suggestions
** deploy to servers script improved readability
** sitenotice endyear analysis and suggestions for strong improvement in ease of use, using existing sitenotice banner mechanic
* 2026-04-28
** Github Link styling presentation upgrades + raw link feature
** wiki table font size bugfix
** widget eagerImage documentation
** 43-footer TOS function improvements
** AI Code review php files + wiki files + js files
* 2026-04-27
** CodeSelect fixes and upgrades
*** Analysis: false but also 0 (falsy values) were both interpreted as boolean and therefore ouput as empty string. *** Solution: Now output as string. false is specifically transformed to string
*** Also all other potentially falsy values were checked, all work now: false, 0, 0.0, null, [], empty string
*** New tests were added to wikitest
*** Documentation was updated
*** Empty string and no parameter warning added
** IntroLike bugfix
*** Analysis: IntroLike was only optimized for multiline content
*** Solution: Now every content is interpreted as mutliline
*** New test added to wikitest
** Github_link template rewrite
*** Analysis: Template was based on ExtLink, which is not good for this use case as admin found by trial
*** Completely new widget was written and template adjusted
*** Now direct repo urls are allowed as input and will be formatted, direct profile urls and file urls which are rendered as 2 links - one to the repo one to the file
*** Documentation was rewritten
*** Wikitests was rewritten

== Archive ==

{{Collapsible
|title=Archive
|smallTitle=true
|content=
}}

= Footnotes =
<references />
{{Footer}}